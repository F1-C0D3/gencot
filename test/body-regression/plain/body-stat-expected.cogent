cogent_sv1 : () -> ()
cogent_sv1 () = let _ = 0 in ()
cogent_sv2 : U32 -> ()
cogent_sv2 i = let _ = 0 in ()
cogent_sv3 : () -> ()
cogent_sv3 () = let _ = 0 in ()
cogent_sv4 : () -> ()
cogent_sv4 () =
   let (_
       , r') = let v' = let v' = ()
         in v'
      in (3, v')
   in ()

cogent_se1 : U32 -> U32
cogent_se1 i =
   let (_
       , r') = let v' = let v' = i
         in v'
      in (3, v')
   in r'
cogent_se2 : U32 -> U32
cogent_se2 i =
   let (_
       , r') = let v' = let v' = i
         and v1' = 5
         and v' = v' + v1'
         and v2' = i
         and v' = v' * v2'
         in v'
      in (3, v')
   in r'
cogent_se3 : U32 -> U32
cogent_se3 i =
   let (_, i, r') = let (c'
                        , i) = let (v1', i) = let v' = 1
            and v1' = i
            and v1' = v1' + v'
            and (v1', i) = (i, v1')
            in (v1', i)
         in (0, i)
      in if c' > 0
         then (c', i, r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', i, r')
   in r'
cogent_se4 : U32 -> U32
cogent_se4 i =
   let (_, i, r') = let (c'
                        , i) = let (v1', i) = let v' = 1
            and v1' = i
            and v1' = v1' + v'
            and (v1', i) = (i, v1')
            in (v1', i)
         in (0, i)
      in if c' > 0
         then (c', i, r')
         else let (c', i, r') = let (c'
                                    , i) = let (v1', i) = let v' = 5
                     and v1' = i
                     and v1' = v'
                     and (v1', i) = (v1', v1')
                     in (v1', i)
                  in (0, i)
               in if c' > 0
                  then (c', i, r')
                  else let (c'
                           , r') = let v' = let v' = i
                           in v'
                        in (3, v')
                     in (c', i, r')
            in (c', i, r')
   in r'
cogent_se5 : U32 -> U32
cogent_se5 i =
   let (_, i, r') = let (c'
                        , i) = let (v1', i) = let v' = 1
            and v1' = i
            and v1' = v1' + v'
            and (v1', i) = (i, v1')
            in (v1', i)
         in (0, i)
      in if c' > 0
         then (c', i, r')
         else let (c', r', i) = let (v1'
                                    , i) = let v' = 1
                  and v1' = i
                  and v1' = v1' + v'
                  and (v1', i) = (v1', v1')
                  in (v1', i)
               in (3, v1', i)
            in (c', i, r')
   in r'

cogent_si1 : ( U32, U32 ) -> U32
cogent_si1 (i, j) =
   let (_
       , r') = let v' = let v' = i
         and v1' = 0
         and v' = v' == v1'
         in v'
      in if v'
         then let (c'
                  , r') = let v' = let v' = j
                  in v'
               in (3, v')
            in (c', r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', r')
   in r'
cogent_si2 : ( U32, U32 ) -> U32
cogent_si2 (i, j) =
   let (_, r') = let (c'
                     , r') = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c'
                     , r') = let v' = let v' = j
                     in v'
                  in (3, v')
               in (c', r')
            else let c' = 0
               in (c', r')
      in if c' > 0
         then (c', r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', r')
   in r'
cogent_si3 : ( U32, U32 ) -> U32
cogent_si3 (i, j) =
   let (_, i, r') = let (c'
                        , i) = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c', i) = let (v1'
                                   , i) = let v' = 10
                     and v1' = i
                     and v1' = v'
                     and (v1', i) = (v1', v1')
                     in (v1', i)
                  in (0, i)
               in (c', i)
            else let (c', i) = let (v1'
                                   , i) = let v' = j
                     and v1' = i
                     and v1' = v1' + v'
                     and (v1', i) = (v1', v1')
                     in (v1', i)
                  in (0, i)
               in (c', i)
      in if c' > 0
         then (c', i, r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', i, r')
   in r'
cogent_si4 : ( U32, U32 ) -> U32
cogent_si4 (i, j) =
   let (_, i, j, r') = let (c'
                           , i
                           , j) = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c', i) = let (v1'
                                   , i) = let v' = 10
                     and v1' = i
                     and v1' = v'
                     and (v1', i) = (v1', v1')
                     in (v1', i)
                  in (0, i)
               in (c', i, j)
            else let (c'
                     , j
                     , i) = let v' = let v' = j
                     and v1' = 0
                     and v' = v' == v1'
                     in v'
                  in if v'
                     then let (c', j) = let (v1'
                                            , j) = let v' = i
                              and v1' = j
                              and v1' = v'
                              and (v1', j) = (v1', v1')
                              in (v1', j)
                           in (0, j)
                        in (c', j, i)
                     else let (c', i) = let (v1'
                                            , i) = let v' = j
                              and v1' = i
                              and v1' = v1' + v'
                              and (v1', i) = (v1', v1')
                              in (v1', i)
                           in (0, i)
                        in (c', j, i)
               in (c', i, j)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , r') = let v' = let v' = i
                  and v1' = j
                  and v' = v' + v1'
                  in v'
               in (3, v')
            in (c', i, j, r')
   in r'
cogent_si5 : ( U32, U32 ) -> U32
cogent_si5 (i, j) =
   let (_, i, j, r') = let (c'
                           , i
                           , j) = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c'
                     , i
                     , j) = let v' = let v' = j
                     and v1' = 0
                     and v' = v' == v1'
                     in v'
                  in if v'
                     then let (c', i) = let (v1'
                                            , i) = let v' = 10
                              and v1' = i
                              and v1' = v'
                              and (v1', i) = (v1', v1')
                              in (v1', i)
                           in (0, i)
                        in (c', i, j)
                     else let (c', j) = let (v1'
                                            , j) = let v' = 10
                              and v1' = j
                              and v1' = v1' + v'
                              and (v1', j) = (v1', v1')
                              in (v1', j)
                           in (0, j)
                        in (c', i, j)
               in (c', i, j)
            else let (c'
                     , j
                     , i) = let v' = let v' = j
                     and v1' = 0
                     and v' = v' == v1'
                     in v'
                  in if v'
                     then let (c', j) = let (v1'
                                            , j) = let v' = i
                              and v1' = j
                              and v1' = v'
                              and (v1', j) = (v1', v1')
                              in (v1', j)
                           in (0, j)
                        in (c', j, i)
                     else let (c', i) = let (v1'
                                            , i) = let v' = j
                              and v1' = i
                              and v1' = v1' + v'
                              and (v1', i) = (v1', v1')
                              in (v1', i)
                           in (0, i)
                        in (c', j, i)
               in (c', i, j)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , r') = let v' = let v' = i
                  and v1' = j
                  and v' = v' + v1'
                  in v'
               in (3, v')
            in (c', i, j, r')
   in r'
cogent_si6 : ( U32, U32 ) -> U32
cogent_si6 (i, j) =
   let (_, i, r', j) = let (c'
                           , i
                           , r'
                           , j) = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c'
                     , i
                     , r'
                     , j) = let v' = let v' = j
                     and v1' = 0
                     and v' = v' == v1'
                     in v'
                  in if v'
                     then let (c', i) = let (v1'
                                            , i) = let v' = 10
                              and v1' = i
                              and v1' = v'
                              and (v1', i) = (v1', v1')
                              in (v1', i)
                           in (0, i)
                        in (c', i, r', j)
                     else let (c', r', j) = let (v1'
                                                , j) = let v' = 10
                              and v1' = j
                              and v1' = v1' + v'
                              and (v1', j) = (v1', v1')
                              in (v1', j)
                           in (3, v1', j)
                        in (c', i, r', j)
               in (c', i, r', j)
            else let (c'
                     , r'
                     , j
                     , i) = let v' = let v' = j
                     and v1' = 0
                     and v' = v' == v1'
                     in v'
                  in if v'
                     then let (c', r', j) = let (v1'
                                                , j) = let v' = i
                              and v1' = j
                              and v1' = v'
                              and (v1', j) = (v1', v1')
                              in (v1', j)
                           in (3, v1', j)
                        in (c', r', j, i)
                     else let (c', i) = let (v1'
                                            , i) = let v' = j
                              and v1' = i
                              and v1' = v1' + v'
                              and (v1', i) = (v1', v1')
                              in (v1', i)
                           in (0, i)
                        in (c', r', j, i)
               in (c', i, r', j)
      in if c' > 0
         then (c', i, r', j)
         else let (c'
                  , r') = let v' = let v' = i
                  and v1' = j
                  and v' = v' + v1'
                  in v'
               in (3, v')
            in (c', i, r', j)
   in r'
cogent_si8 : ( U32, U32 ) -> U32
cogent_si8 (i, j) =
   let (_, j, i, r') = let (c'
                           , j
                           , i) = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c', j) = let (v1'
                                   , j) = let v' = 1
                     and v1' = j
                     and v1' = v1' + v'
                     and (v1', j) = (j, v1')
                     in (v1', j)
                  in (0, j)
               in (c', j, i)
            else let (c', i) = let (v1'
                                   , i) = let v' = 1
                     and v1' = i
                     and v1' = v1' + v'
                     and (v1', i) = (i, v1')
                     in (v1', i)
                  in (0, i)
               in (c', j, i)
      in if c' > 0
         then (c', j, i, r')
         else let (c', j, r') = let (c'
                                    , j) = let (v2', j) = let v' = 2
                     and v1' = j
                     and v' = v' * v1'
                     and v2' = j
                     and v2' = v'
                     and (v2', j) = (v2', v2')
                     in (v2', j)
                  in (0, j)
               in if c' > 0
                  then (c', j, r')
                  else let (c'
                           , r') = let v' = let v' = 2
                           and v1' = i
                           and v' = v' * v1'
                           in v'
                        in (3, v')
                     in (c', j, r')
            in (c', j, i, r')
   in r'
cogent_si9 : ( U32, U32 ) -> U32
cogent_si9 (i, j) =
   let (_, j, i, r') = let (c'
                           , j
                           , i) = let v' = let v' = i
            and v1' = 0
            and v' = v' == v1'
            in v'
         in if v'
            then let (c', j) = let (v1'
                                   , j) = let v' = 1
                     and v1' = j
                     and v1' = v1' + v'
                     and (v1', j) = (j, v1')
                     in (v1', j)
                  in (0, j)
               in (c', j, i)
            else let (c', i) = let (v1'
                                   , i) = let v' = 1
                     and v1' = i
                     and v1' = v1' + v'
                     and (v1', i) = (i, v1')
                     in (v1', i)
                  in (0, i)
               in (c', j, i)
      in if c' > 0
         then (c', j, i, r')
         else let (c', i, r') = let (c'
                                    , i) = let (v2', i) = let v' = 2
                     and v1' = j
                     and v' = v' * v1'
                     and v2' = i
                     and v2' = v'
                     and (v2', i) = (v2', v2')
                     in (v2', i)
                  in (0, i)
               in if c' > 0
                  then (c', i, r')
                  else let (c'
                           , r') = let v' = let v' = 2
                           and v1' = j
                           and v' = v' * v1'
                           in v'
                        in (3, v')
                     in (c', i, r')
            in (c', j, i, r')
   in r'

cogent_sc1 : ( U32, U32 ) -> U32
cogent_sc1 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c', j) = let (v2'
                                            , j) = let v' = 2
                     and v1' = i
                     and v' = v' * v1'
                     and v2' = j
                     and v2' = v2' + v'
                     and (v2', j) = (v2', v2')
                     in (v2', j)
                  in (0, j)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c', i, r') = let (c'
                                             , i) = let (v2', i) = let v' = i
                              and v1' = j
                              and v' = v' + v1'
                              and v2' = i
                              and v2' = v'
                              and (v2', i) = (v2', v2')
                              in (v2', i)
                           in (0, i)
                        in if c' > 0
                           then (c', i, r')
                           else let (c'
                                    , r') = let v' = let v' = j
                                    and v1' = i
                                    and v' = v' * v1'
                                    in v'
                                 in (3, v')
                              in (c', i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'
cogent_sc2 : ( U32, U32 ) -> U32
cogent_sc2 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j
                              , i) = let v' = let v' = i
                     and v1' = 13
                     and v' = v' > v1'
                     in v'
                  in if v'
                     then let (c', j, i) = let (c'
                                               , j) = let (v2', j) = let v' = 2
                                 and v1' = i
                                 and v' = v' * v1'
                                 and v2' = j
                                 and v2' = v2' + v'
                                 and (v2', j) = (v2', v2')
                                 in (v2', j)
                              in (0, j)
                           in if c' > 0
                              then (c', j, i)
                              else let (c', i) = let (v2'
                                                     , i) = let v' = 2
                                       and v1' = j
                                       and v' = v' * v1'
                                       and v2' = i
                                       and v2' = v2' + v'
                                       and (v2', i) = (v2', v2')
                                       in (v2', i)
                                    in (0, i)
                                 in (c', j, i)
                        in (c', j, i)
                     else let (c', i, j) = let (c'
                                               , i) = let (v1', i) = let v' = 1
                                 and v1' = i
                                 and v1' = v1' + v'
                                 and (v1', i) = (i, v1')
                                 in (v1', i)
                              in (0, i)
                           in if c' > 0
                              then (c', i, j)
                              else let (c', j) = let (v1'
                                                     , j) = let v' = 1
                                       and v1' = j
                                       and v1' = v1' - v'
                                       and (v1', j) = (j, v1')
                                       in (v1', j)
                                    in (0, j)
                                 in (c', i, j)
                        in (c', j, i)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c'
                           , j
                           , i
                           , r') = let (c'
                                       , j) = let v' = let v' = j
                              and v1' = 0
                              and v' = v' == v1'
                              in v'
                           in if v'
                              then let (c', j) = let (v1'
                                                     , j) = let v' = 15
                                       and v1' = j
                                       and v1' = v'
                                       and (v1', j) = (v1', v1')
                                       in (v1', j)
                                    in (0, j)
                                 in (c', j)
                              else let c' = 0
                                 in (c', j)
                        in if c' > 0
                           then (c', j, i, r')
                           else let (c', i, r') = let (c'
                                                      , i) = let (v1'
                                                                 , i) = let v' = j
                                       and v1' = i
                                       and v1' = v1' + v'
                                       and (v1', i) = (v1', v1')
                                       in (v1', i)
                                    in (0, i)
                                 in if c' > 0
                                    then (c', i, r')
                                    else let (c'
                                             , r') = let v' = let v' = i
                                             in v'
                                          in (3, v')
                                       in (c', i, r')
                              in (c', j, i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'

cogent_sd1 : ( U32, U32 ) -> U32
cogent_sd1 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , r') = let v' = let v' = j
                  and v1' = 7
                  and v' = v' + v1'
                  in v'
               and (c', j, r') = let k = v'
                  and (c', j, k, r') = let (c'
                                           , j) = let (v2', j) = let v' = i
                           and v1' = k
                           and v' = v' + v1'
                           and v2' = j
                           and v2' = v2' + v'
                           and (v2', j) = (v2', v2')
                           in (v2', j)
                        in (0, j)
                     in if c' > 0
                        then (c', j, k, r')
                        else let (c', k, r') = let (c'
                                                   , k) = let (v2'
                                                              , k) = let v' = j
                                    and v1' = i
                                    and v' = v' + v1'
                                    and v2' = k
                                    and v2' = v2' + v'
                                    and (v2', k) = (v2', v2')
                                    in (v2', k)
                                 in (0, k)
                              in if c' > 0
                                 then (c', k, r')
                                 else let (c'
                                          , r') = let v' = let v' = k
                                          in v'
                                       in (3, v')
                                    in (c', k, r')
                           in (c', j, k, r')
                  in (c', j, r')
               in (c', j, r')
            in (c', i, j, r')
   in r'
cogent_sd2 : ( U32, U32 ) -> U32
cogent_sd2 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c', j, r') = let (c'
                                    , j) = let v' = let v' = j
                     and v1' = 7
                     and v' = v' + v1'
                     in v'
                  and (c', j) = let i = v'
                     and (c', j, i) = let (c'
                                          , j) = let (v2', j) = let v' = i
                              and v1' = i
                              and v' = v' + v1'
                              and v2' = j
                              and v2' = v2' + v'
                              and (v2', j) = (v2', v2')
                              in (v2', j)
                           in (0, j)
                        in if c' > 0
                           then (c', j, i)
                           else let (c', i) = let (v2'
                                                  , i) = let v' = j
                                    and v1' = i
                                    and v' = v' + v1'
                                    and v2' = i
                                    and v2' = v2' + v'
                                    and (v2', i) = (v2', v2')
                                    in (v2', i)
                                 in (0, i)
                              in (c', j, i)
                     in (c', j)
                  in (c', j)
               in if c' > 0
                  then (c', j, r')
                  else let (c'
                           , r') = let v' = let v' = i
                           in v'
                        in (3, v')
                     in (c', j, r')
            in (c', i, j, r')
   in r'
cogent_sd3 : ( U32, U32 ) -> U32
cogent_sd3 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j) = let v' = let v' = j
                     and v1' = 7
                     and v' = v' + v1'
                     in v'
                  and (c', j) = let k = v'
                     and (c', j, k) = let (c'
                                          , j) = let (v2', j) = let v' = i
                              and v1' = k
                              and v' = v' + v1'
                              and v2' = j
                              and v2' = v2' + v'
                              and (v2', j) = (v2', v2')
                              in (v2', j)
                           in (0, j)
                        in if c' > 0
                           then (c', j, k)
                           else let (c', k) = let (v1'
                                                  , k) = let v' = 15
                                    and v1' = k
                                    and v1' = v'
                                    and (v1', k) = (v1', v1')
                                    in (v1', k)
                                 in (0, k)
                              in (c', j, k)
                     in (c', j)
                  in (c', j)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c', i, r') = let (c'
                                             , i) = let (v2', i) = let v' = j
                              and v1' = i
                              and v' = v' + v1'
                              and v2' = i
                              and v2' = v2' + v'
                              and (v2', i) = (v2', v2')
                              in (v2', i)
                           in (0, i)
                        in if c' > 0
                           then (c', i, r')
                           else let (c'
                                    , r') = let v' = let v' = i
                                    in v'
                                 in (3, v')
                              in (c', i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'
cogent_sd4 : ( U32, U32 ) -> U32
cogent_sd4 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j) = let v' = let v' = i
                     and v1' = 0
                     and v' = v' == v1'
                     in v'
                  in if v'
                     then let (c'
                              , j) = let v' = let v' = j
                              and v1' = 7
                              and v' = v' + v1'
                              in v'
                           and (c', j) = let k = v'
                              and (c', j, k) = let (c'
                                                   , j) = let (v2'
                                                              , j) = let v' = i
                                       and v1' = k
                                       and v' = v' + v1'
                                       and v2' = j
                                       and v2' = v2' + v'
                                       and (v2', j) = (v2', v2')
                                       in (v2', j)
                                    in (0, j)
                                 in if c' > 0
                                    then (c', j, k)
                                    else let (c', k) = let (v1'
                                                           , k) = let v' = 15
                                             and v1' = k
                                             and v1' = v'
                                             and (v1', k) = (v1', v1')
                                             in (v1', k)
                                          in (0, k)
                                       in (c', j, k)
                              in (c', j)
                           in (c', j)
                        in (c', j)
                     else let (c', j) = let (v2'
                                            , j) = let v' = i
                              and v1' = 3
                              and v' = v' + v1'
                              and v2' = j
                              and v2' = v'
                              and (v2', j) = (v2', v2')
                              in (v2', j)
                           in (0, j)
                        in (c', j)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c', i, r') = let (c'
                                             , i) = let (v2', i) = let v' = j
                              and v1' = i
                              and v' = v' + v1'
                              and v2' = i
                              and v2' = v2' + v'
                              and (v2', i) = (v2', v2')
                              in (v2', i)
                           in (0, i)
                        in if c' > 0
                           then (c', i, r')
                           else let (c'
                                    , r') = let v' = let v' = i
                                    in v'
                                 in (3, v')
                              in (c', i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'
cogent_sd5 : ( U32, U32 ) -> U32
cogent_sd5 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , r') = let v' = let v' = defaultVal[U32]{{}} ()
                  in v'
               and (c', j, r') = let k1 = v'
                  and (c'
                      , k1
                      , j
                      , r') = let v' = let v' = defaultVal[U32]{{}} ()
                        in v'
                     and (c'
                         , k1
                         , j
                         , r') = let k2 = v'
                        and (c'
                            , k1
                            , k2
                            , j
                            , r') = let (c', k1) = let (v2'
                                                       , k1) = let v' = i
                                 and v1' = 1
                                 and v' = v' + v1'
                                 and v2' = k1
                                 and v2' = v'
                                 and (v2', k1) = (v2', v2')
                                 in (v2', k1)
                              in (0, k1)
                           in if c' > 0
                              then (c', k1, k2, j, r')
                              else let (c'
                                       , k2
                                       , j
                                       , k1
                                       , r') = let (c', k2) = let (v2'
                                                                  , k2) = let v' = j
                                          and v1' = 1
                                          and v' = v' + v1'
                                          and v2' = k2
                                          and v2' = v'
                                          and (v2', k2) = (v2', v2')
                                          in (v2', k2)
                                       in (0, k2)
                                    in if c' > 0
                                       then (c', k2, j, k1, r')
                                       else let (c'
                                                , j
                                                , k1
                                                , r') = let (c', j) = let (v2'
                                                                          , j) = let v' = k1
                                                   and v1' = k2
                                                   and v' = v' + v1'
                                                   and v2' = j
                                                   and v2' = v2' + v'
                                                   and (v2', j) = (v2', v2')
                                                   in (v2', j)
                                                in (0, j)
                                             in if c' > 0
                                                then (c', j, k1, r')
                                                else let (c', k1, r') = let (c'
                                                                            , k1) = let (v2'
                                                                                        , k1) = let v' = j
                                                            and v1' = i
                                                            and v' = v' + v1'
                                                            and v2' = k1
                                                            and v2' = v2' + v'
                                                            and (v2', k1) = (v2'
                                                                            , v2')
                                                            in (v2', k1)
                                                         in (0, k1)
                                                      in if c' > 0
                                                         then (c', k1, r')
                                                         else let (c'
                                                                  , r') = let v' = let v' = k1
                                                                  in v'
                                                               in (3, v')
                                                            in (c', k1, r')
                                                   in (c', j, k1, r')
                                          in (c', k2, j, k1, r')
                                 in (c', k1, k2, j, r')
                        in (c', k1, j, r')
                     in (c', k1, j, r')
                  in (c', j, r')
               in (c', j, r')
            in (c', i, j, r')
   in r'
cogent_sd6 : ( U32, U32 ) -> U32
cogent_sd6 (i, j) =
   let (_, i, j, r') = let (c'
                           , i) = let (v2', i) = let v' = 2
            and v1' = j
            and v' = v' * v1'
            and v2' = i
            and v2' = v2' + v'
            and (v2', i) = (v2', v2')
            in (v2', i)
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , r') = let v' = let v' = defaultVal[U32]{{}} ()
                  in v'
               and (c', j, r') = let k1 = v'
                  and (c'
                      , k1
                      , j
                      , r') = let v' = let v' = 42
                        in v'
                     and (c'
                         , k1
                         , j
                         , r') = let k2 = v'
                        and (c', k1, j, r') = let (c'
                                                  , k1) = let (v2'
                                                              , k1) = let v' = i
                                 and v1' = 1
                                 and v' = v' + v1'
                                 and v2' = k1
                                 and v2' = v'
                                 and (v2', k1) = (v2', v2')
                                 in (v2', k1)
                              in (0, k1)
                           in if c' > 0
                              then (c', k1, j, r')
                              else let (c'
                                       , j
                                       , k1
                                       , r') = let (c', j) = let (v2'
                                                                 , j) = let v' = k1
                                          and v1' = k2
                                          and v' = v' + v1'
                                          and v2' = j
                                          and v2' = v2' + v'
                                          and (v2', j) = (v2', v2')
                                          in (v2', j)
                                       in (0, j)
                                    in if c' > 0
                                       then (c', j, k1, r')
                                       else let (c', k1, r') = let (c'
                                                                   , k1) = let (v2'
                                                                               , k1) = let v' = j
                                                   and v1' = i
                                                   and v' = v' + v1'
                                                   and v2' = k1
                                                   and v2' = v2' + v'
                                                   and (v2', k1) = (v2', v2')
                                                   in (v2', k1)
                                                in (0, k1)
                                             in if c' > 0
                                                then (c', k1, r')
                                                else let (c'
                                                         , r') = let v' = let v' = k1
                                                         in v'
                                                      in (3, v')
                                                   in (c', k1, r')
                                          in (c', j, k1, r')
                                 in (c', k1, j, r')
                        in (c', k1, j, r')
                     in (c', k1, j, r')
                  in (c', j, r')
               in (c', j, r')
            in (c', i, j, r')
   in r'

cogent_ss1 : U32 -> U32
cogent_ss1 i =
   let (_
       , r') = let s' = let v' = i
         in v'
      and s1' = s' == (let v' = 0
            in v')
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c'
                     , r') = let v' = let v' = i
                     in v'
                  in (3, v')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let c' = 0
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss2 : U32 -> U32
cogent_ss2 i =
   let (_
       , r') = let s' = let v' = i
         in v'
      and s1' = True
      and (c', r') = let (c'
                         , r') = let (c'
                                     , r') = let v' = let v' = 0
                  in v'
               in (3, v')
            in (c', r')
         in if c' > 0
            then (c', r')
            else let c' = 0
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss3 : U32 -> U32
cogent_ss3 i =
   let (_
       , r') = let s' = let v' = i
         in v'
      and (s1'
          , s2') = (s' == (let v' = 0
                      in v')
                   , True)
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c'
                     , r') = let v' = let v' = i
                     in v'
                  in (3, v')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let (c', r') = let (c'
                                    , r') = let (c'
                                                , r') = let v' = let v' = 0
                           in v'
                        in (3, v')
                     in (c', r')
                  in if c' > 0
                     then (c', r')
                     else let c' = 0
                        in (c', r')
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss4 : U32 -> U32
cogent_ss4 i =
   let (_
       , r') = let s' = let v' = i
         in v'
      and (s1'
          , s2'
          , s3') = (s' == (let v' = 0
                      in v')
                   , s' == (let v' = 1
                      in v')
                   , True)
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c'
                     , r') = let v' = let v' = i
                     in v'
                  in (3, v')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let (c', r') = let (c'
                                    , r') = if s1' || s2'
                     then let (c'
                              , r') = let v' = let v' = i
                              and v1' = 5
                              and v' = v' + v1'
                              in v'
                           in (3, v')
                        in (c', r')
                     else (0, r')
                  in if c' > 0
                     then (c', r')
                     else let (c', r') = let (c'
                                             , r') = let (c'
                                                         , r') = let v' = let v' = 0
                                    in v'
                                 in (3, v')
                              in (c', r')
                           in if c' > 0
                              then (c', r')
                              else let c' = 0
                                 in (c', r')
                        in (c', r')
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss5 : U32 -> U32
cogent_ss5 i =
   let (_
       , r') = let s' = let v' = i
         in v'
      and (s1'
          , s2'
          , s3') = (s' == (let v' = 0
                      in v')
                   , True
                   , s' == (let v' = 1
                      in v'))
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c'
                     , r') = let v' = let v' = i
                     in v'
                  in (3, v')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let (c', r') = let (c'
                                    , r') = if not s3'
                     then let (c'
                              , r') = let v' = let v' = 0
                              in v'
                           in (3, v')
                        in (c', r')
                     else (0, r')
                  in if c' > 0
                     then (c', r')
                     else let (c', r') = let (c'
                                             , r') = let (c'
                                                         , r') = let v' = let v' = i
                                    and v1' = 5
                                    and v' = v' + v1'
                                    in v'
                                 in (3, v')
                              in (c', r')
                           in if c' > 0
                              then (c', r')
                              else let c' = 0
                                 in (c', r')
                        in (c', r')
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss6 : ( U32, U32 ) -> U32
cogent_ss6 (i, j) =
   let (_
       , j
       , i
       , r') = let s' = let v' = i
         in v'
      and (s1'
          , s2'
          , s3'
          , s4') = (s' == (let v' = 0
                      in v')
                   , s' == (let v' = 1
                      in v')
                   , s' == (let v' = 2
                      in v')
                   , True)
      and (c', j, i, r') = let (c'
                               , j) = if s1'
            then let (c', j) = let (v1'
                                   , j) = let v' = i
                     and v1' = j
                     and v1' = v'
                     and (v1', j) = (v1', v1')
                     in (v1', j)
                  in (0, j)
               in (c', j)
            else (0, j)
         in if c' > 0
            then (c', j, i, r')
            else let (c'
                     , i
                     , j
                     , r') = let (c'
                                 , i) = if s1' || s2'
                     then let (c', i) = let (v1'
                                            , i) = let v' = 1
                              and v1' = i
                              and v1' = v1' + v'
                              and (v1', i) = (i, v1')
                              in (v1', i)
                           in (0, i)
                        in (c', i)
                     else (0, i)
                  in if c' > 0
                     then (c', i, j, r')
                     else let (c', j, r') = let (c'
                                                , j) = if s1' || s2' || s3'
                              then let (c', j) = let (v1'
                                                     , j) = let v' = 5
                                       and v1' = j
                                       and v1' = v'
                                       and (v1', j) = (v1', v1')
                                       in (v1', j)
                                    in (0, j)
                                 in (c', j)
                              else (0, j)
                           in if c' > 0
                              then (c', j, r')
                              else let (c', r') = let (c'
                                                      , r') = let (c'
                                                                  , r') = let v' = let v' = j
                                             in v'
                                          in (3, v')
                                       in (c', r')
                                    in if c' > 0
                                       then (c', r')
                                       else let c' = 0
                                          in (c', r')
                                 in (c', j, r')
                        in (c', i, j, r')
               in (c', j, i, r')
      in (if c' == 2 then 0 else c'
         , j
         , i
         , r')
   in r'

cogent_ss7 : ( U32, U32 ) -> U32
cogent_ss7 (i, j) =
   let (_, j, i, r') = let (c'
                           , j
                           , i) = let s' = let v' = i in v'
         and (s1'
             , s2'
             , s3') = (s' == (let v' = 0
                         in v')
                      , s' == (let v' = 1
                         in v')
                      , s' == (let v' = 2
                         in v'))
         and (c', j, i) = let (c'
                              , j) = if s1'
               then let (c', j) = let (v1'
                                      , j) = let v' = i
                        and v1' = j
                        and v1' = v'
                        and (v1', j) = (v1', v1')
                        in (v1', j)
                     in (0, j)
                  in (c', j)
               else (0, j)
            in if c' > 0
               then (c', j, i)
               else let (c', i, j) = let (c'
                                         , i) = if s1' || s2'
                        then let (c', i) = let (c'
                                               , i) = let (v1', i) = let v' = 1
                                    and v1' = i
                                    and v1' = v1' + v'
                                    and (v1', i) = (i, v1')
                                    in (v1', i)
                                 in (0, i)
                              in if c' > 0
                                 then (c', i)
                                 else let c' = 2
                                    in (c', i)
                           in (c', i)
                        else (0, i)
                     in if c' > 0
                        then (c', i, j)
                        else let (c', j) = let (c'
                                               , j) = if s1' || s2' || s3'
                                 then let (c', j) = let (v1'
                                                        , j) = let v' = 5
                                          and v1' = j
                                          and v1' = v'
                                          and (v1', j) = (v1', v1')
                                          in (v1', j)
                                       in (0, j)
                                    in (c', j)
                                 else (0, j)
                              in if c' > 0
                                 then (c', j)
                                 else let c' = 0
                                    in (c', j)
                           in (c', i, j)
                  in (c', j, i)
         in (if c' == 2 then 0 else c'
            , j
            , i)
      in if c' > 0
         then (c', j, i, r')
         else let (c'
                  , r') = let v' = let v' = j
                  in v'
               in (3, v')
            in (c', j, i, r')
   in r'

cogent_ss8 : ( U32, U32 ) -> U32
cogent_ss8 (i, j) =
   let (_, j, r') = let (c'
                        , j
                        , r') = let s' = let v' = i
            in v'
         and (s1'
             , s2'
             , s3') = (s' == (let v' = 0
                         in v')
                      , s' == (let v' = 1
                         in v')
                      , s' == (let v' = 2
                         in v'))
         and (c', j, r') = let (c'
                               , j) = if s1'
               then let (c', j) = let (v1'
                                      , j) = let v' = i
                        and v1' = j
                        and v1' = v'
                        and (v1', j) = (v1', v1')
                        in (v1', j)
                     in (0, j)
                  in (c', j)
               else (0, j)
            in if c' > 0
               then (c', j, r')
               else let (c', r', j) = let (c'
                                          , r') = if s1' || s2'
                        then let (c'
                                 , r') = let s' = let v' = j
                                 in v'
                              and (s1'
                                  , s2') = (s' == (let v' = 0
                                              in v')
                                           , True)
                              and (c', r') = let (c'
                                                 , r') = if s1'
                                    then let (c'
                                             , r') = let v' = let v' = 17
                                             in v'
                                          in (3, v')
                                       in (c', r')
                                    else (0, r')
                                 in if c' > 0
                                    then (c', r')
                                    else let (c', r') = let (c'
                                                            , r') = let (c'
                                                                        , r') = let v' = let v' = i
                                                   in v'
                                                in (3, v')
                                             in (c', r')
                                          in if c' > 0
                                             then (c', r')
                                             else let c' = 0
                                                in (c', r')
                                       in (c', r')
                              in (if c' == 2 then 0 else c'
                                 , r')
                           in (c', r')
                        else (0, r')
                     in if c' > 0
                        then (c', r', j)
                        else let (c', j) = let (c'
                                               , j) = if s1' || s2' || s3'
                                 then let (c', j) = let (v1'
                                                        , j) = let v' = 5
                                          and v1' = j
                                          and v1' = v'
                                          and (v1', j) = (v1', v1')
                                          in (v1', j)
                                       in (0, j)
                                    in (c', j)
                                 else (0, j)
                              in if c' > 0
                                 then (c', j)
                                 else let c' = 0
                                    in (c', j)
                           in (c', r', j)
                  in (c', j, r')
         in (if c' == 2 then 0 else c'
            , j
            , r')
      in if c' > 0
         then (c', j, r')
         else let (c'
                  , r') = let v' = let v' = j
                  in v'
               in (3, v')
            in (c', j, r')
   in r'

cogent_ss10 : U32 -> U32
cogent_ss10 i =
   let _ = let s' = let v' = i
         in v'
      and () = ()
      and c' = gencotDummy "Unsupported switch body"
         {- return i; -}
      in if c' == 2 then 0 else c'
   in r'

cogent_ss11 : U32 -> U32
cogent_ss11 i =
   let (_
       , r') = let c' = let s' = let v' = i
            in v'
         and () = ()
         and c' = gencotDummy "Unsupported switch body"
            {- if (i == 0)
                break; -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', r')
   in r'

cogent_ss12 : U32 -> U32
cogent_ss12 i =
   let (_
       , r') = let c' = let s' = let v' = i
            in v'
         and () = ()
         and c' = gencotDummy "Unsupported switch body"
            {- i = 5; -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', r')
   in r'

cogent_ss13 : ( U32, U32 ) -> U32
cogent_ss13 (i, j) =
   let (_
       , j
       , r') = let s' = let v' = i
         in v'
      and (s1'
          , s2') = (s' == (let v' = 0
                      in v')
                   , True)
      and (c', j, r') = let (c'
                            , j) = if s1'
            then let (c', j) = let (c'
                                   , j) = let (v1', j) = let v' = i
                        and v1' = j
                        and v1' = v'
                        and (v1', j) = (v1', v1')
                        in (v1', j)
                     in (0, j)
                  in if c' > 0
                     then (c', j)
                     else let (c'
                              , j) = let v' = let v' = i
                              and v1' = 5
                              and v' = v' < v1'
                              in v'
                           in if v'
                              then let (c', j) = let (c'
                                                     , j) = let (v1'
                                                                , j) = let v' = 1
                                          and v1' = j
                                          and v1' = v1' + v'
                                          and (v1', j) = (j, v1')
                                          in (v1', j)
                                       in (0, j)
                                    in if c' > 0
                                       then (c', j)
                                       else
                                          let c' = gencotDummy "Case statement only supported in direct switch body"
                                             {- 
                                             case 3:
                                             j++; -}
                                          in (c', j)
                                 in (c', j)
                              else
                                 let c' = let c' = gencotDummy "Case statement only supported in direct switch body"
                                       {- 
                                       case 1:
                                       return i + 5; -}
                                    in if c' > 0
                                       then c'
                                       else
                                          let c' = gencotDummy "Case statement only supported in direct switch body"
                                             {- 
                                             case 7:
                                             j--; -}
                                          in c'
                                 in (c', j)
                        in (c', j)
               in (c', j)
            else (0, j)
         in if c' > 0
            then (c', j, r')
            else let (c', r') = let (c'
                                    , r') = let (c'
                                                , r') = let v' = let v' = 0
                           in v'
                        in (3, v')
                     in (c', r')
                  in if c' > 0
                     then (c', r')
                     else let c' = 0
                        in (c', r')
               in (c', j, r')
      in (if c' == 2 then 0 else c'
         , j
         , r')
   in r'

cogent_ss14 : U32 -> U32
cogent_ss14 i =
   let (_
       , r') = let c' = let s' = let v' = i
            in v'
         and (s1'
             , s2'
             , s3') = (s' == (let v' = 0
                         in v')
                      , s' == (let v' = 1
                         in v')
                      , s' == (let v' = 2
                         in v'))
         and c' = gencotDummy "Declarations not supported in switch body"
            {-
                U32 j = 0;
                
                
              case 0:
                j = i;
                
              case 1:
                i++;
                break;
                
              case 2:
                j = 5;
            -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c'
                  , r') = let v' = let v' = i
                  in v'
               in (3, v')
            in (c', r')
   in r'

cogent_ss15 : ( U32, U32 ) -> U32
cogent_ss15 (i, j) =
   let (_
       , r') = let c' = let s' = let v' = i
            in v'
         and (s1'
             , s2'
             , s3') = (s' == (let v' = 0
                         in v')
                      , s' == (let v' = 1
                         in v')
                      , s' == (let v' = 2
                         in v'))
         and c' = gencotDummy "Declarations not supported in switch body"
            {-
                
              case 0:
                j = i;
                
                U32 k = 0;
                
                
              case 1:
                i++;
                break;
                
              case 2:
                j = k;
            -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c'
                  , r') = let v' = let v' = j
                  in v'
               in (3, v')
            in (c', r')
   in r'
