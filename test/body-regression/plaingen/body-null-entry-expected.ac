$ty:(MayNull (CPtr U32)) fn1($ty:(U32) i, $ty:(MayNull (CPtr U32)) p)
{
    $ty:(( U32, MayNull (CPtr U32) )) arg = {.p1 =i, .p2 =p};
    
    return cogent_fn1(arg).p1;
}
$ty:(MayNull (CPtr U32)) fn2($ty:(U32) i, $ty:(CPtr U32) p)
{
    $ty:(( U32, CPtr U32 )) arg = {.p1 =i, .p2 =p};
    
    return cogent_fn2(arg).p1;
}
$ty:(U32) fcnl($ty:(MayNull (CPtr U32)) i)
{
    return cogent_fcnl(i);
}
$ty:(U32) fcnn($ty:(CPtr U32) i)
{
    return cogent_fcnn(i);
}
$ty:(MayNull (CPtr U32)) globcnl;
$ty:(CPtr U32) globcnn;
$ty:(CPtr U32) globnn;
$ty:(MayNull (CPtr U32)) globnl;
$ty:(U32) fn11nl($ty:(CPtr U32) pnn)
{
    return cogent_fn11nl(pnn).p1;
}
$ty:(U32) fn11nn($ty:(CPtr U32) pnn)
{
    return cogent_fn11nn(pnn).p1;
}
$ty:(U32) fn12nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn12nl(arg);
}
$ty:(U32) fn12nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn12nn(arg);
}
$ty:(U32) fn13nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn13nl(arg);
}
$ty:(U32) fn13nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn13nn(arg);
}
$ty:(U32) fn14nl($ty:(CPtr U32) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( CPtr U32, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_fn14nl(arg).p1;
}
$ty:(U32) fn14nn($ty:(CPtr U32) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( CPtr U32, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_fn14nn(arg).p1;
}
void fn21nl(void )
{
    cogent_fn21nl(&globcnl);
}
void fn21nn(void )
{
    cogent_fn21nn(&globcnn);
}
void fn22nl($ty:(MayNull (CPtr U32)) pnl)
{
    cogent_fn22nl(pnl);
}
$ty:(U32) fn22nn($ty:(CPtr U32) pnn)
{
    return cogent_fn22nn(pnn).p1;
}
$ty:(MayNull (CPtr U32)) fn31nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn31nl(arg);
}
$ty:(CPtr U32) fn31nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn31nn(arg);
}
$ty:(U32) fn41nl($ty:(CPtr U32) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( CPtr U32, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_fn41nl(arg).p1;
}
$ty:(U32) fn42nl($ty:(MayNull (CPtr U32)) pnl, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, U32 )) arg = {.p1 =pnl, .p2 =qnn, .p3 =i};
    
    return cogent_fn42nl(arg).p1;
}
$ty:(MayNull (CPtr U32)) fn51nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_fn51nl(pnn).p1;
}
$ty:(CPtr U32) fn51nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_fn51nn(pnn).p1;
}
$ty:(U32) fn52nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_fn52nl(pnn).p1;
}
$ty:(U32) fn52nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_fn52nn(pnn).p1;
}
void fn53nl($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_fn53nl(arg);
}
void fn53nn($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_fn53nn(arg);
}
void fn54nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_fn54nl(arg);
}
$ty:(U32) fn54nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_fn54nn(arg).p1;
}
$ty:(U32) fn55nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_fn55nl(arg).p1;
}
$ty:(U32) fn55nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_fn55nn(arg).p1;
}
$ty:(U32) fn61nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_fn61nn(pnn).p1;
}
$ty:(U32) fn62nn($ty:(Struct_Cogent_nns2) pnn)
{
    return cogent_fn62nn(pnn).p1;
}
$ty:(U32) fn63nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn63nn(arg);
}
$ty:(U32) fn64nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_fn64nn(arg);
}
$ty:(MayNull (CPtr U32)) fn71nl($ty:(Cogent_nna2) pnn)
{
    return cogent_fn71nl(pnn).p1;
}
$ty:(CPtr U32) fn71nn($ty:(Cogent_nna2) pnn)
{
    return cogent_fn71nn(pnn).p1;
}
$ty:(U32) fn72nl($ty:(Cogent_nna2) pnn)
{
    return cogent_fn72nl(pnn).p1;
}
$ty:(U32) fn72nn($ty:(Cogent_nna2) pnn)
{
    return cogent_fn72nn(pnn).p1;
}
void fn73nl($ty:(Cogent_nna2) pnn)
{
    $ty:(( Cogent_nna2, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_fn73nl(arg);
}
void fn73nn($ty:(Cogent_nna2) pnn)
{
    $ty:(( Cogent_nna2, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_fn73nn(arg);
}
void fn74nl($ty:(Cogent_nna2) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Cogent_nna2, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_fn74nl(arg);
}
$ty:(U32) fn74nn($ty:(Cogent_nna2) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Cogent_nna2, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_fn74nn(arg).p1;
}
$ty:(U32) fn75nl($ty:(Cogent_nna2) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Cogent_nna2, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_fn75nl(arg).p1;
}
$ty:(U32) fn75nn($ty:(Cogent_nna2) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Cogent_nna2, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_fn75nn(arg).p1;
}
$ty:(U32) fn81nn($ty:(Cogent_nna1) pnn)
{
    return cogent_fn81nn(pnn).p1;
}
$ty:(U32) fn82nn($ty:(Cogent_nna5) pnn)
{
    return cogent_fn82nn(pnn).p1;
}
void fn91nl($ty:(Struct_Cogent_nns1) pnn)
{
    cogent_fn91nl(pnn);
}
void fn91nn($ty:(Struct_Cogent_nns1) pnn)
{
    cogent_fn91nn(pnn);
}
void fn92nl($ty:(Cogent_nna3) pnl)
{
    cogent_fn92nl(pnl);
}
void fn92nn($ty:(Cogent_nna2) pnl)
{
    cogent_fn92nn(pnl);
}
$ty:(U32) gn11nl($ty:(MayNull (CPtr U32)) pnl)
{
    return cogent_gn11nl(pnl).p1;
}
$ty:(U32) gn11nn($ty:(MayNull (CPtr U32)) pnl)
{
    return cogent_gn11nn(pnl).p1;
}
$ty:(U32) gn12nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn12nl(arg);
}
$ty:(U32) gn12nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn12nn(arg);
}
$ty:(U32) gn13nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn13nl(arg);
}
$ty:(U32) gn13nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn13nn(arg);
}
$ty:(U32) gn14nl($ty:(MayNull (CPtr U32)) pnl, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), MayNull (CPtr U32), U32 )) arg = {.p1 =pnl, .p2 =qnl, .p3 =i};
    
    return cogent_gn14nl(arg).p1;
}
$ty:(U32) gn14nn($ty:(MayNull (CPtr U32)) pnl, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), MayNull (CPtr U32), U32 )) arg = {.p1 =pnl, .p2 =qnl, .p3 =i};
    
    return cogent_gn14nn(arg).p1;
}
void gn21nl(void )
{
    cogent_gn21nl(&globcnl);
}
void gn21nn(void )
{
    cogent_gn21nn(&globcnn);
}
void gn22nl($ty:(MayNull (CPtr U32)) pnl)
{
    cogent_gn22nl(pnl);
}
$ty:(U32) gn22nn($ty:(CPtr U32) pnn)
{
    return cogent_gn22nn(pnn).p1;
}
$ty:(MayNull (CPtr U32)) gn31nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn31nl(arg);
}
$ty:(CPtr U32) gn31nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn31nn(arg);
}
$ty:(MayNull (CPtr U32)) gn51nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_gn51nl(pnn).p1;
}
$ty:(CPtr U32) gn51nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_gn51nn(pnn).p1;
}
$ty:(U32) gn52nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_gn52nl(pnn).p1;
}
$ty:(U32) gn52nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_gn52nn(pnn).p1;
}
void gn53nl($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_gn53nl(arg);
}
void gn53nn($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_gn53nn(arg);
}
void gn54nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_gn54nl(arg);
}
$ty:(U32) gn54nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_gn54nn(arg).p1;
}
$ty:(U32) gn55nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_gn55nl(arg).p1;
}
$ty:(U32) gn55nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_gn55nn(arg).p1;
}
$ty:(U32) gn61nl($ty:(MayNull Struct_Cogent_nns1) pnl)
{
    return cogent_gn61nl(pnl).p1;
}
$ty:(U32) gn62nl($ty:(Struct_Cogent_nns2) pnn)
{
    return cogent_gn62nl(pnn).p1;
}
$ty:(U32) gn63nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn63nl(arg);
}
$ty:(U32) gn64nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_gn64nl(arg);
}
$ty:(MayNull (CPtr U32)) gn71nl($ty:(Cogent_nna3) pnn)
{
    return cogent_gn71nl(pnn).p1;
}
$ty:(CPtr U32) gn71nn($ty:(Cogent_nna3) pnn)
{
    return cogent_gn71nn(pnn).p1;
}
$ty:(U32) gn72nl($ty:(Cogent_nna3) pnn)
{
    return cogent_gn72nl(pnn).p1;
}
$ty:(U32) gn72nn($ty:(Cogent_nna3) pnn)
{
    return cogent_gn72nn(pnn).p1;
}
void gn73nl($ty:(Cogent_nna3) pnn)
{
    $ty:(( Cogent_nna3, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_gn73nl(arg);
}
void gn73nn($ty:(Cogent_nna3) pnn)
{
    $ty:(( Cogent_nna3, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_gn73nn(arg);
}
void gn74nl($ty:(Cogent_nna3) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_gn74nl(arg);
}
$ty:(U32) gn74nn($ty:(Cogent_nna3) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Cogent_nna3, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_gn74nn(arg).p1;
}
$ty:(U32) gn75nl($ty:(Cogent_nna3) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_gn75nl(arg).p1;
}
$ty:(U32) gn75nn($ty:(Cogent_nna3) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Cogent_nna3, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_gn75nn(arg).p1;
}
$ty:(U32) gn81nl($ty:(Cogent_nna1) pnl)
{
    return cogent_gn81nl(pnl).p1;
}
$ty:(U32) gn82nl($ty:(Cogent_nna4) pnn)
{
    return cogent_gn82nl(pnn).p1;
}
void gn91nl($ty:(Struct_Cogent_nns1) pnn)
{
    cogent_gn91nl(pnn);
}
void gn91nn($ty:(Struct_Cogent_nns1) pnn)
{
    cogent_gn91nn(pnn);
}
void gn92nl($ty:(Cogent_nna3) pnl)
{
    cogent_gn92nl(pnl);
}
void gn92nn($ty:(Cogent_nna2) pnl)
{
    cogent_gn92nn(pnl);
}
$ty:(U32) tn11nl($ty:(MayNull (CPtr U32)) pnl)
{
    return cogent_tn11nl(pnl).p1;
}
$ty:(U32) tn11nn($ty:(MayNull (CPtr U32)) pnl)
{
    return cogent_tn11nn(pnl).p1;
}
$ty:(U32) tn12nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_tn12nl(arg);
}
$ty:(U32) tn12nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_tn12nn(arg);
}
$ty:(U32) tn13nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_tn13nl(arg);
}
$ty:(U32) tn13nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_tn13nn(arg);
}
$ty:(U32) tn14nl($ty:(MayNull (CPtr U32)) pnl, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, U32 )) arg = {.p1 =pnl, .p2 =qnn, .p3 =i};
    
    return cogent_tn14nl(arg).p1;
}
$ty:(U32) tn14nn($ty:(MayNull (CPtr U32)) pnl, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, U32 )) arg = {.p1 =pnl, .p2 =qnn, .p3 =i};
    
    return cogent_tn14nn(arg).p1;
}
void tn21nl($ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, GlobState1 )) arg = {.p1 =qnl, .p2 =qnn, .p3 =&globcnl};
    
    cogent_tn21nl(arg);
}
void tn21nn($ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, GlobState2 )) arg = {.p1 =qnl, .p2 =qnn, .p3 =&globcnn};
    
    cogent_tn21nn(arg);
}
void tn22nl($ty:(MayNull (CPtr U32)) pnl, $ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =pnl, .p2 =qnl, .p3 =qnn};
    
    cogent_tn22nl(arg);
}
$ty:(U32) tn22nn($ty:(MayNull (CPtr U32)) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =qnn};
    
    return cogent_tn22nn(arg).p1;
}
$ty:(MayNull (CPtr U32)) tn31nl($ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =qnl, .p2 =qnn};
    
    return cogent_tn31nl(arg).p1;
}
$ty:(CPtr U32) tn31nn($ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =qnl, .p2 =qnn};
    
    return cogent_tn31nn(arg).p1;
}
$ty:(MayNull (CPtr U32)) tn51nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_tn51nl(pnn).p1;
}
$ty:(CPtr U32) tn51nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_tn51nn(pnn).p1;
}
$ty:(U32) tn52nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_tn52nl(pnn).p1;
}
$ty:(U32) tn52nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_tn52nn(pnn).p1;
}
void tn53nl($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_tn53nl(arg);
}
void tn53nn($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_tn53nn(arg);
}
void tn54nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_tn54nl(arg);
}
$ty:(U32) tn54nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_tn54nn(arg).p1;
}
$ty:(U32) tn55nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_tn55nl(arg).p1;
}
$ty:(U32) tn55nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_tn55nn(arg).p1;
}
$ty:(U32) tn61nl($ty:(MayNull Struct_Cogent_nns1) pnl, $ty:(MayNull Struct_Cogent_nns1) pnn)
{
    $ty:(( MayNull Struct_Cogent_nns1, MayNull Struct_Cogent_nns1 )) arg = {.p1 =pnl, .p2 =pnn};
    
    return cogent_tn61nl(arg).p1;
}
$ty:(U32) tn62nl($ty:(Struct_Cogent_nns2) pnn)
{
    return cogent_tn62nl(pnn).p1;
}
$ty:(U32) tn63nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_tn63nl(arg);
}
$ty:(U32) tn64nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_tn64nl(arg);
}
$ty:(MayNull (CPtr U32)) tn71nl($ty:(Cogent_nna3) pnn)
{
    return cogent_tn71nl(pnn).p1;
}
$ty:(CPtr U32) tn71nn($ty:(Cogent_nna3) pnn)
{
    return cogent_tn71nn(pnn).p1;
}
$ty:(U32) tn72nl($ty:(Cogent_nna3) pnn)
{
    return cogent_tn72nl(pnn).p1;
}
$ty:(U32) tn72nn($ty:(Cogent_nna3) pnn)
{
    return cogent_tn72nn(pnn).p1;
}
void tn73nl($ty:(Cogent_nna3) pnn)
{
    $ty:(( Cogent_nna3, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_tn73nl(arg);
}
void tn73nn($ty:(Cogent_nna3) pnn)
{
    $ty:(( Cogent_nna3, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_tn73nn(arg);
}
void tn74nl($ty:(Cogent_nna3) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_tn74nl(arg);
}
$ty:(U32) tn74nn($ty:(Cogent_nna3) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Cogent_nna3, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_tn74nn(arg).p1;
}
$ty:(U32) tn75nl($ty:(Cogent_nna3) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_tn75nl(arg).p1;
}
$ty:(U32) tn75nn($ty:(Cogent_nna3) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Cogent_nna3, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_tn75nn(arg).p1;
}
$ty:(U32) tn81nl($ty:(Cogent_nna1) pnl, $ty:(Cogent_nna1) qnn)
{
    $ty:(( Cogent_nna1, Cogent_nna1 )) arg = {.p1 =pnl, .p2 =qnn};
    
    return cogent_tn81nl(arg).p1;
}
$ty:(U32) tn82nl($ty:(Cogent_nna4) pnn, $ty:(Cogent_nna1) qnn)
{
    $ty:(( Cogent_nna4, Cogent_nna1 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_tn82nl(arg).p1;
}
void tn91nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =qnn};
    
    cogent_tn91nl(arg);
}
void tn91nn($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =qnn};
    
    cogent_tn91nn(arg);
}
void tn92nl($ty:(Cogent_nna3) pnl, $ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =pnl, .p2 =qnl, .p3 =qnn};
    
    cogent_tn92nl(arg);
}
void tn92nn($ty:(Cogent_nna2) pnl, $ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( Cogent_nna2, MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =pnl, .p2 =qnl, .p3 =qnn};
    
    cogent_tn92nn(arg);
}
$ty:(U32) sn11nl($ty:(MayNull (CPtr U32)) pnl)
{
    return cogent_sn11nl(pnl).p1;
}
$ty:(U32) sn11nn($ty:(MayNull (CPtr U32)) pnl)
{
    return cogent_sn11nn(pnl).p1;
}
$ty:(U32) sn12nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_sn12nl(arg);
}
$ty:(U32) sn12nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_sn12nn(arg);
}
$ty:(U32) sn13nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_sn13nl(arg);
}
$ty:(U32) sn13nn(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_sn13nn(arg);
}
$ty:(U32) sn14nl($ty:(MayNull (CPtr U32)) pnl, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, U32 )) arg = {.p1 =pnl, .p2 =qnn, .p3 =i};
    
    return cogent_sn14nl(arg).p1;
}
$ty:(U32) sn14nn($ty:(MayNull (CPtr U32)) pnl, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( MayNull (CPtr U32), CPtr U32, U32 )) arg = {.p1 =pnl, .p2 =qnn, .p3 =i};
    
    return cogent_sn14nn(arg).p1;
}
void sn21nl($ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( MayNull (CPtr U32), GlobState1 )) arg = {.p1 =qnl, .p2 =&globcnl};
    
    cogent_sn21nl(arg);
}
void sn21nn($ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( MayNull (CPtr U32), GlobState2 )) arg = {.p1 =qnl, .p2 =&globcnn};
    
    cogent_sn21nn(arg);
}
void sn22nl($ty:(MayNull (CPtr U32)) pnl, $ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( MayNull (CPtr U32), MayNull (CPtr U32) )) arg = {.p1 =pnl, .p2 =qnl};
    
    cogent_sn22nl(arg);
}
$ty:(U32) sn22nn($ty:(MayNull (CPtr U32)) pnn, $ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( MayNull (CPtr U32), MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =qnl};
    
    return cogent_sn22nn(arg).p1;
}
$ty:(MayNull (CPtr U32)) sn31nl($ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =qnl, .p2 =qnn};
    
    return cogent_sn31nl(arg).p1;
}
$ty:(CPtr U32) sn31nn($ty:(MayNull (CPtr U32)) qnl, $ty:(CPtr U32) qnn)
{
    $ty:(( MayNull (CPtr U32), CPtr U32 )) arg = {.p1 =qnl, .p2 =qnn};
    
    return cogent_sn31nn(arg).p1;
}
$ty:(MayNull (CPtr U32)) sn51nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_sn51nl(pnn).p1;
}
$ty:(CPtr U32) sn51nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_sn51nn(pnn).p1;
}
$ty:(U32) sn52nl($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_sn52nl(pnn).p1;
}
$ty:(U32) sn52nn($ty:(Struct_Cogent_nns1) pnn)
{
    return cogent_sn52nn(pnn).p1;
}
void sn53nl($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_sn53nl(arg);
}
void sn53nn($ty:(Struct_Cogent_nns1) pnn)
{
    $ty:(( Struct_Cogent_nns1, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_sn53nn(arg);
}
void sn54nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_sn54nl(arg);
}
$ty:(U32) sn54nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_sn54nn(arg).p1;
}
$ty:(U32) sn55nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_sn55nl(arg).p1;
}
$ty:(U32) sn55nn($ty:(Struct_Cogent_nns1) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Struct_Cogent_nns1, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_sn55nn(arg).p1;
}
$ty:(U32) sn61nl($ty:(MayNull Struct_Cogent_nns1) pnl)
{
    return cogent_sn61nl(pnl).p1;
}
$ty:(U32) sn62nl($ty:(MayNull Struct_Cogent_nns2) pnn)
{
    return cogent_sn62nl(pnn).p1;
}
$ty:(U32) sn63nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_sn63nl(arg);
}
$ty:(U32) sn64nl(void )
{
    $ty:(()) arg = {.dummy =0};
    
    return cogent_sn64nl(arg);
}
$ty:(MayNull (CPtr U32)) sn71nl($ty:(Cogent_nna3) pnn)
{
    return cogent_sn71nl(pnn).p1;
}
$ty:(CPtr U32) sn71nn($ty:(Cogent_nna3) pnn)
{
    return cogent_sn71nn(pnn).p1;
}
$ty:(U32) sn72nl($ty:(Cogent_nna3) pnn)
{
    return cogent_sn72nl(pnn).p1;
}
$ty:(U32) sn72nn($ty:(Cogent_nna3) pnn)
{
    return cogent_sn72nn(pnn).p1;
}
void sn73nl($ty:(Cogent_nna3) pnn)
{
    $ty:(( Cogent_nna3, GlobState1 )) arg = {.p1 =pnn, .p2 =&globcnl};
    
    cogent_sn73nl(arg);
}
void sn73nn($ty:(Cogent_nna3) pnn)
{
    $ty:(( Cogent_nna3, GlobState2 )) arg = {.p1 =pnn, .p2 =&globcnn};
    
    cogent_sn73nn(arg);
}
void sn74nl($ty:(Cogent_nna3) pnn, $ty:(MayNull (CPtr U32)) pnl)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =pnl};
    
    cogent_sn74nl(arg);
}
$ty:(U32) sn74nn($ty:(Cogent_nna3) pnn, $ty:(CPtr U32) qnn)
{
    $ty:(( Cogent_nna3, CPtr U32 )) arg = {.p1 =pnn, .p2 =qnn};
    
    return cogent_sn74nn(arg).p1;
}
$ty:(U32) sn75nl($ty:(Cogent_nna3) pnn, $ty:(MayNull (CPtr U32)) qnl, $ty:(U32) i)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32), U32 )) arg = {.p1 =pnn, .p2 =qnl, .p3 =i};
    
    return cogent_sn75nl(arg).p1;
}
$ty:(U32) sn75nn($ty:(Cogent_nna3) pnn, $ty:(CPtr U32) qnn, $ty:(U32) i)
{
    $ty:(( Cogent_nna3, CPtr U32, U32 )) arg = {.p1 =pnn, .p2 =qnn, .p3 =i};
    
    return cogent_sn75nn(arg).p1;
}
$ty:(U32) sn81nl($ty:(Cogent_nna1) pnl)
{
    return cogent_sn81nl(pnl).p1;
}
$ty:(U32) sn82nl($ty:(Cogent_nna4) pnn)
{
    return cogent_sn82nl(pnn).p1;
}
void sn91nl($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =qnl};
    
    cogent_sn91nl(arg);
}
void sn91nn($ty:(Struct_Cogent_nns1) pnn, $ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( Struct_Cogent_nns1, MayNull (CPtr U32) )) arg = {.p1 =pnn, .p2 =qnl};
    
    cogent_sn91nn(arg);
}
void sn92nl($ty:(Cogent_nna3) pnl, $ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( Cogent_nna3, MayNull (CPtr U32) )) arg = {.p1 =pnl, .p2 =qnl};
    
    cogent_sn92nl(arg);
}
void sn92nn($ty:(Cogent_nna2) pnl, $ty:(MayNull (CPtr U32)) qnl)
{
    $ty:(( Cogent_nna2, MayNull (CPtr U32) )) arg = {.p1 =pnl, .p2 =qnl};
    
    cogent_sn92nn(arg);
}
