void sv1(void )
{
    $ty:(()) arg = {.dummy =0};
    
    cogent_sv1(arg);
}
void sv2($ty:(U32) i)
{
    cogent_sv2(i);
}
void sv3(void )
{
    $ty:(()) arg = {.dummy =0};
    
    cogent_sv3(arg);
}
void sv4(void )
{
    $ty:(()) arg = {.dummy =0};
    
    cogent_sv4(arg);
}
$ty:(U32) se1($ty:(U32) i)
{
    return cogent_se1(i);
}
$ty:(U32) se2($ty:(U32) i)
{
    return cogent_se2(i);
}
$ty:(U32) se3($ty:(U32) i)
{
    return cogent_se3(i);
}
$ty:(U32) se4($ty:(U32) i)
{
    return cogent_se4(i);
}
$ty:(U32) se5($ty:(U32) i)
{
    return cogent_se5(i);
}
$ty:(U32) si1($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si1(arg);
}
$ty:(U32) si2($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si2(arg);
}
$ty:(U32) si3($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si3(arg);
}
$ty:(U32) si4($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si4(arg);
}
$ty:(U32) si5($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si5(arg);
}
$ty:(U32) si6($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si6(arg);
}
$ty:(U32) si8($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si8(arg);
}
$ty:(U32) si9($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_si9(arg);
}
$ty:(U32) sc1($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sc1(arg);
}
$ty:(U32) sc2($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sc2(arg);
}
$ty:(U32) sd1($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sd1(arg);
}
$ty:(U32) sd2($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sd2(arg);
}
$ty:(U32) sd3($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sd3(arg);
}
$ty:(U32) sd4($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sd4(arg);
}
$ty:(U32) sd5($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sd5(arg);
}
$ty:(U32) sd6($ty:(U32) i, $ty:(U32) j)
{
    $ty:(( U32, U32 )) arg = {.p1 =i, .p2 =j};
    
    return cogent_sd6(arg);
}
