cogent_sv1 : () -> ()
cogent_sv1 () = let c' = 0 in ()
cogent_sv2 : U32 -> ()
cogent_sv2 i = let c' = 0 in ()
cogent_sv3 : () -> ()
cogent_sv3 () = let c' = 0 in ()
cogent_sv4 : () -> ()
cogent_sv4 () =
   let (c', r') = let v' = ()
      in (3, v')
   in ()

cogent_se1 : U32 -> U32
cogent_se1 i =
   let (c', r') = let v' = i
      in (3, v')
   in r'
cogent_se2 : U32 -> U32
cogent_se2 i =
   let (c', r') = let v' = i
      and v1' = 5
      and v2' = v' + v1'
      and v3' = i
      and v4' = v2' * v3'
      in (3, v4')
   in r'
cogent_se3 : U32 -> U32
cogent_se3 i =
   let (c', i, r') = let (c'
                         , i) = let v' = 1
         and v1' = i
         and v2' = v1' + v'
         and (v3', i) = (i, v2')
         in (0, i)
      in if c' > 0
         then (c', i, r')
         else let (c', r') = let v4' = i
               in (3, v4')
            in (c', i, r')
   in r'
cogent_se4 : U32 -> U32
cogent_se4 i =
   let (c', i, r') = let (c'
                         , i) = let v' = 1
         and v1' = i
         and v2' = v1' + v'
         and (v3', i) = (i, v2')
         in (0, i)
      in if c' > 0
         then (c', i, r')
         else let (c', i, r') = let (c'
                                    , i) = let v4' = 5
                  and v5' = i
                  and v6' = v4'
                  and (v7', i) = (v6', v6')
                  in (0, i)
               in if c' > 0
                  then (c', i, r')
                  else let (c', r') = let v8' = i
                        in (3, v8')
                     in (c', i, r')
            in (c', i, r')
   in r'
cogent_se5 : U32 -> U32
cogent_se5 i =
   let (c', i, r') = let (c'
                         , i) = let v' = 1
         and v1' = i
         and v2' = v1' + v'
         and (v3', i) = (i, v2')
         in (0, i)
      in if c' > 0
         then (c', i, r')
         else let (c'
                  , r'
                  , i) = let v4' = 1
               and v5' = i
               and v6' = v5' + v4'
               and (v7', i) = (v6', v6')
               in (3, v7', i)
            in (c', i, r')
   in r'

cogent_si1 : ( U32, U32 ) -> U32
cogent_si1 (i, j) =
   let (c', r') = let v' = i
      and v1' = 0
      and v2' = v' == v1'
      in if v2'
         then let (c', r') = let v3' = j
               in (3, v3')
            in (c', r')
         else let (c', r') = let v4' = i
               in (3, v4')
            in (c', r')
   in r'
cogent_si2 : ( U32, U32 ) -> U32
cogent_si2 (i, j) =
   let (c', r') = let (c'
                      , r') = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c', r') = let v3' = j
                  in (3, v3')
               in (c', r')
            else let c' = 0
               in (c', r')
      in if c' > 0
         then (c', r')
         else let (c', r') = let v4' = i
               in (3, v4')
            in (c', r')
   in r'
cogent_si3 : ( U32, U32 ) -> U32
cogent_si3 (i, j) =
   let (c', i, r') = let (c'
                         , i) = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c', i) = let v3' = 10
                  and v4' = i
                  and v5' = v3'
                  and (v6', i) = (v5', v5')
                  in (0, i)
               in (c', i)
            else let (c', i) = let v7' = j
                  and v8' = i
                  and v9' = v8' + v7'
                  and (v10', i) = (v9', v9')
                  in (0, i)
               in (c', i)
      in if c' > 0
         then (c', i, r')
         else let (c', r') = let v11' = i
               in (3, v11')
            in (c', i, r')
   in r'
cogent_si4 : ( U32, U32 ) -> U32
cogent_si4 (i, j) =
   let (c', i, j, r') = let (c'
                            , i
                            , j) = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c', i) = let v3' = 10
                  and v4' = i
                  and v5' = v3'
                  and (v6', i) = (v5', v5')
                  in (0, i)
               in (c', i, j)
            else let (c'
                     , j
                     , i) = let v7' = j
                  and v8' = 0
                  and v9' = v7' == v8'
                  in if v9'
                     then let (c', j) = let v10' = i
                           and v11' = j
                           and v12' = v10'
                           and (v13', j) = (v12', v12')
                           in (0, j)
                        in (c', j, i)
                     else let (c', i) = let v14' = j
                           and v15' = i
                           and v16' = v15' + v14'
                           and (v17', i) = (v16', v16')
                           in (0, i)
                        in (c', j, i)
               in (c', i, j)
      in if c' > 0
         then (c', i, j, r')
         else let (c', r') = let v18' = i
               and v19' = j
               and v20' = v18' + v19'
               in (3, v20')
            in (c', i, j, r')
   in r'
cogent_si5 : ( U32, U32 ) -> U32
cogent_si5 (i, j) =
   let (c', i, j, r') = let (c'
                            , i
                            , j) = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c'
                     , i
                     , j) = let v3' = j
                  and v4' = 0
                  and v5' = v3' == v4'
                  in if v5'
                     then let (c', i) = let v6' = 10
                           and v7' = i
                           and v8' = v6'
                           and (v9', i) = (v8', v8')
                           in (0, i)
                        in (c', i, j)
                     else let (c', j) = let v10' = 10
                           and v11' = j
                           and v12' = v11' + v10'
                           and (v13', j) = (v12', v12')
                           in (0, j)
                        in (c', i, j)
               in (c', i, j)
            else let (c'
                     , j
                     , i) = let v14' = j
                  and v15' = 0
                  and v16' = v14' == v15'
                  in if v16'
                     then let (c', j) = let v17' = i
                           and v18' = j
                           and v19' = v17'
                           and (v20', j) = (v19', v19')
                           in (0, j)
                        in (c', j, i)
                     else let (c', i) = let v21' = j
                           and v22' = i
                           and v23' = v22' + v21'
                           and (v24', i) = (v23', v23')
                           in (0, i)
                        in (c', j, i)
               in (c', i, j)
      in if c' > 0
         then (c', i, j, r')
         else let (c', r') = let v25' = i
               and v26' = j
               and v27' = v25' + v26'
               in (3, v27')
            in (c', i, j, r')
   in r'
cogent_si6 : ( U32, U32 ) -> U32
cogent_si6 (i, j) =
   let (c', i, r', j) = let (c'
                            , i
                            , r'
                            , j) = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c'
                     , i
                     , r'
                     , j) = let v3' = j
                  and v4' = 0
                  and v5' = v3' == v4'
                  in if v5'
                     then let (c', i) = let v6' = 10
                           and v7' = i
                           and v8' = v6'
                           and (v9', i) = (v8', v8')
                           in (0, i)
                        in (c', i, r', j)
                     else let (c'
                              , r'
                              , j) = let v10' = 10
                           and v11' = j
                           and v12' = v11' + v10'
                           and (v13', j) = (v12', v12')
                           in (3, v13', j)
                        in (c', i, r', j)
               in (c', i, r', j)
            else let (c'
                     , r'
                     , j
                     , i) = let v14' = j
                  and v15' = 0
                  and v16' = v14' == v15'
                  in if v16'
                     then let (c'
                              , r'
                              , j) = let v17' = i
                           and v18' = j
                           and v19' = v17'
                           and (v20', j) = (v19', v19')
                           in (3, v20', j)
                        in (c', r', j, i)
                     else let (c', i) = let v21' = j
                           and v22' = i
                           and v23' = v22' + v21'
                           and (v24', i) = (v23', v23')
                           in (0, i)
                        in (c', r', j, i)
               in (c', i, r', j)
      in if c' > 0
         then (c', i, r', j)
         else let (c', r') = let v25' = i
               and v26' = j
               and v27' = v25' + v26'
               in (3, v27')
            in (c', i, r', j)
   in r'
cogent_si8 : ( U32, U32 ) -> U32
cogent_si8 (i, j) =
   let (c', j, i, r') = let (c'
                            , j
                            , i) = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c', j) = let v3' = 1
                  and v4' = j
                  and v5' = v4' + v3'
                  and (v6', j) = (j, v5')
                  in (0, j)
               in (c', j, i)
            else let (c', i) = let v7' = 1
                  and v8' = i
                  and v9' = v8' + v7'
                  and (v10', i) = (i, v9')
                  in (0, i)
               in (c', j, i)
      in if c' > 0
         then (c', j, i, r')
         else let (c', j, r') = let (c'
                                    , j) = let v11' = 2
                  and v12' = j
                  and v13' = v11' * v12'
                  and v14' = j
                  and v15' = v13'
                  and (v16', j) = (v15', v15')
                  in (0, j)
               in if c' > 0
                  then (c', j, r')
                  else let (c', r') = let v17' = 2
                        and v18' = i
                        and v19' = v17' * v18'
                        in (3, v19')
                     in (c', j, r')
            in (c', j, i, r')
   in r'
cogent_si9 : ( U32, U32 ) -> U32
cogent_si9 (i, j) =
   let (c', j, i, r') = let (c'
                            , j
                            , i) = let v' = i
         and v1' = 0
         and v2' = v' == v1'
         in if v2'
            then let (c', j) = let v3' = 1
                  and v4' = j
                  and v5' = v4' + v3'
                  and (v6', j) = (j, v5')
                  in (0, j)
               in (c', j, i)
            else let (c', i) = let v7' = 1
                  and v8' = i
                  and v9' = v8' + v7'
                  and (v10', i) = (i, v9')
                  in (0, i)
               in (c', j, i)
      in if c' > 0
         then (c', j, i, r')
         else let (c', i, r') = let (c'
                                    , i) = let v11' = 2
                  and v12' = j
                  and v13' = v11' * v12'
                  and v14' = i
                  and v15' = v13'
                  and (v16', i) = (v15', v15')
                  in (0, i)
               in if c' > 0
                  then (c', i, r')
                  else let (c', r') = let v17' = 2
                        and v18' = j
                        and v19' = v17' * v18'
                        in (3, v19')
                     in (c', i, r')
            in (c', j, i, r')
   in r'

cogent_sc1 : ( U32, U32 ) -> U32
cogent_sc1 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j) = let v6' = 2
                  and v7' = i
                  and v8' = v6' * v7'
                  and v9' = j
                  and v10' = v9' + v8'
                  and (v11', j) = (v10', v10')
                  in (0, j)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c', i, r') = let (c'
                                             , i) = let v12' = i
                           and v13' = j
                           and v14' = v12' + v13'
                           and v15' = i
                           and v16' = v14'
                           and (v17', i) = (v16', v16')
                           in (0, i)
                        in if c' > 0
                           then (c', i, r')
                           else let (c', r') = let v18' = j
                                 and v19' = i
                                 and v20' = v18' * v19'
                                 in (3, v20')
                              in (c', i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'
cogent_sc2 : ( U32, U32 ) -> U32
cogent_sc2 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j
                              , i) = let v6' = i
                  and v7' = 13
                  and v8' = v6' > v7'
                  in if v8'
                     then let (c', j, i) = let (c'
                                               , j) = let v9' = 2
                              and v10' = i
                              and v11' = v9' * v10'
                              and v12' = j
                              and v13' = v12' + v11'
                              and (v14', j) = (v13', v13')
                              in (0, j)
                           in if c' > 0
                              then (c', j, i)
                              else let (c', i) = let v15' = 2
                                    and v16' = j
                                    and v17' = v15' * v16'
                                    and v18' = i
                                    and v19' = v18' + v17'
                                    and (v20', i) = (v19', v19')
                                    in (0, i)
                                 in (c', j, i)
                        in (c', j, i)
                     else let (c', i, j) = let (c'
                                               , i) = let v21' = 1
                              and v22' = i
                              and v23' = v22' + v21'
                              and (v24', i) = (i, v23')
                              in (0, i)
                           in if c' > 0
                              then (c', i, j)
                              else let (c', j) = let v25' = 1
                                    and v26' = j
                                    and v27' = v26' - v25'
                                    and (v28', j) = (j, v27')
                                    in (0, j)
                                 in (c', i, j)
                        in (c', j, i)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c'
                           , j
                           , i
                           , r') = let (c'
                                       , j) = let v29' = j
                           and v30' = 0
                           and v31' = v29' == v30'
                           in if v31'
                              then let (c', j) = let v32' = 15
                                    and v33' = j
                                    and v34' = v32'
                                    and (v35', j) = (v34', v34')
                                    in (0, j)
                                 in (c', j)
                              else let c' = 0
                                 in (c', j)
                        in if c' > 0
                           then (c', j, i, r')
                           else let (c', i, r') = let (c'
                                                      , i) = let v36' = j
                                    and v37' = i
                                    and v38' = v37' + v36'
                                    and (v39', i) = (v38', v38')
                                    in (0, i)
                                 in if c' > 0
                                    then (c', i, r')
                                    else let (c', r') = let v40' = i
                                          in (3, v40')
                                       in (c', i, r')
                              in (c', j, i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'

cogent_sd1 : ( U32, U32 ) -> U32
cogent_sd1 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , r') = let v6' = j
               and v7' = 7
               and v8' = v6' + v7'
               and (c', j, r') = let k = v8'
                  and (c', j, k, r') = let (c'
                                           , j) = let v9' = i
                        and v10' = k
                        and v11' = v9' + v10'
                        and v12' = j
                        and v13' = v12' + v11'
                        and (v14', j) = (v13', v13')
                        in (0, j)
                     in if c' > 0
                        then (c', j, k, r')
                        else let (c', k, r') = let (c'
                                                   , k) = let v15' = j
                                 and v16' = i
                                 and v17' = v15' + v16'
                                 and v18' = k
                                 and v19' = v18' + v17'
                                 and (v20', k) = (v19', v19')
                                 in (0, k)
                              in if c' > 0
                                 then (c', k, r')
                                 else let (c', r') = let v21' = k
                                       in (3, v21')
                                    in (c', k, r')
                           in (c', j, k, r')
                  in (c', j, r')
               in (c', j, r')
            in (c', i, j, r')
   in r'
cogent_sd2 : ( U32, U32 ) -> U32
cogent_sd2 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c', j, r') = let (c'
                                    , j) = let v6' = j
                  and v7' = 7
                  and v8' = v6' + v7'
                  and (c', j) = let i = v8'
                     and (c', j, i) = let (c'
                                          , j) = let v9' = i
                           and v10' = i
                           and v11' = v9' + v10'
                           and v12' = j
                           and v13' = v12' + v11'
                           and (v14', j) = (v13', v13')
                           in (0, j)
                        in if c' > 0
                           then (c', j, i)
                           else let (c', i) = let v15' = j
                                 and v16' = i
                                 and v17' = v15' + v16'
                                 and v18' = i
                                 and v19' = v18' + v17'
                                 and (v20', i) = (v19', v19')
                                 in (0, i)
                              in (c', j, i)
                     in (c', j)
                  in (c', j)
               in if c' > 0
                  then (c', j, r')
                  else let (c', r') = let v21' = i
                        in (3, v21')
                     in (c', j, r')
            in (c', i, j, r')
   in r'
cogent_sd3 : ( U32, U32 ) -> U32
cogent_sd3 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j) = let v6' = j
                  and v7' = 7
                  and v8' = v6' + v7'
                  and (c', j) = let k = v8'
                     and (c', j, k) = let (c'
                                          , j) = let v9' = i
                           and v10' = k
                           and v11' = v9' + v10'
                           and v12' = j
                           and v13' = v12' + v11'
                           and (v14', j) = (v13', v13')
                           in (0, j)
                        in if c' > 0
                           then (c', j, k)
                           else let (c', k) = let v15' = 15
                                 and v16' = k
                                 and v17' = v15'
                                 and (v18', k) = (v17', v17')
                                 in (0, k)
                              in (c', j, k)
                     in (c', j)
                  in (c', j)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c', i, r') = let (c'
                                             , i) = let v19' = j
                           and v20' = i
                           and v21' = v19' + v20'
                           and v22' = i
                           and v23' = v22' + v21'
                           and (v24', i) = (v23', v23')
                           in (0, i)
                        in if c' > 0
                           then (c', i, r')
                           else let (c', r') = let v25' = i
                                 in (3, v25')
                              in (c', i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'
cogent_sd4 : ( U32, U32 ) -> U32
cogent_sd4 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , i
                  , r') = let (c'
                              , j) = let v6' = i
                  and v7' = 0
                  and v8' = v6' == v7'
                  in if v8'
                     then let (c', j) = let v9' = j
                           and v10' = 7
                           and v11' = v9' + v10'
                           and (c', j) = let k = v11'
                              and (c', j, k) = let (c'
                                                   , j) = let v12' = i
                                    and v13' = k
                                    and v14' = v12' + v13'
                                    and v15' = j
                                    and v16' = v15' + v14'
                                    and (v17', j) = (v16', v16')
                                    in (0, j)
                                 in if c' > 0
                                    then (c', j, k)
                                    else let (c', k) = let v18' = 15
                                          and v19' = k
                                          and v20' = v18'
                                          and (v21', k) = (v20', v20')
                                          in (0, k)
                                       in (c', j, k)
                              in (c', j)
                           in (c', j)
                        in (c', j)
                     else let (c', j) = let v22' = i
                           and v23' = 3
                           and v24' = v22' + v23'
                           and v25' = j
                           and v26' = v24'
                           and (v27', j) = (v26', v26')
                           in (0, j)
                        in (c', j)
               in if c' > 0
                  then (c', j, i, r')
                  else let (c', i, r') = let (c'
                                             , i) = let v28' = j
                           and v29' = i
                           and v30' = v28' + v29'
                           and v31' = i
                           and v32' = v31' + v30'
                           and (v33', i) = (v32', v32')
                           in (0, i)
                        in if c' > 0
                           then (c', i, r')
                           else let (c', r') = let v34' = i
                                 in (3, v34')
                              in (c', i, r')
                     in (c', j, i, r')
            in (c', i, j, r')
   in r'
cogent_sd5 : ( U32, U32 ) -> U32
cogent_sd5 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , r') = let v6' = defaultVal[U32]{{}} ()
               and (c', j, r') = let k1 = v6'
                  and (c'
                      , k1
                      , j
                      , r') = let v7' = defaultVal[U32]{{}} ()
                     and (c'
                         , k1
                         , j
                         , r') = let k2 = v7'
                        and (c'
                            , k1
                            , k2
                            , j
                            , r') = let (c'
                                        , k1) = let v8' = i
                              and v9' = 1
                              and v10' = v8' + v9'
                              and v11' = k1
                              and v12' = v10'
                              and (v13', k1) = (v12', v12')
                              in (0, k1)
                           in if c' > 0
                              then (c', k1, k2, j, r')
                              else let (c'
                                       , k2
                                       , j
                                       , k1
                                       , r') = let (c'
                                                   , k2) = let v14' = j
                                       and v15' = 1
                                       and v16' = v14' + v15'
                                       and v17' = k2
                                       and v18' = v16'
                                       and (v19', k2) = (v18', v18')
                                       in (0, k2)
                                    in if c' > 0
                                       then (c', k2, j, k1, r')
                                       else let (c'
                                                , j
                                                , k1
                                                , r') = let (c'
                                                            , j) = let v20' = k1
                                                and v21' = k2
                                                and v22' = v20' + v21'
                                                and v23' = j
                                                and v24' = v23' + v22'
                                                and (v25', j) = (v24', v24')
                                                in (0, j)
                                             in if c' > 0
                                                then (c', j, k1, r')
                                                else let (c', k1, r') = let (c'
                                                                            , k1) = let v26' = j
                                                         and v27' = i
                                                         and v28' = v26' + v27'
                                                         and v29' = k1
                                                         and v30' = v29' + v28'
                                                         and (v31', k1) = (v30'
                                                                          , v30')
                                                         in (0, k1)
                                                      in if c' > 0
                                                         then (c', k1, r')
                                                         else let (c'
                                                                  , r') = let v32' = k1
                                                               in (3, v32')
                                                            in (c', k1, r')
                                                   in (c', j, k1, r')
                                          in (c', k2, j, k1, r')
                                 in (c', k1, k2, j, r')
                        in (c', k1, j, r')
                     in (c', k1, j, r')
                  in (c', j, r')
               in (c', j, r')
            in (c', i, j, r')
   in r'
cogent_sd6 : ( U32, U32 ) -> U32
cogent_sd6 (i, j) =
   let (c', i, j, r') = let (c'
                            , i) = let v' = 2
         and v1' = j
         and v2' = v' * v1'
         and v3' = i
         and v4' = v3' + v2'
         and (v5', i) = (v4', v4')
         in (0, i)
      in if c' > 0
         then (c', i, j, r')
         else let (c'
                  , j
                  , r') = let v6' = defaultVal[U32]{{}} ()
               and (c', j, r') = let k1 = v6'
                  and (c'
                      , k1
                      , j
                      , r') = let v7' = 42
                     and (c'
                         , k1
                         , j
                         , r') = let k2 = v7'
                        and (c', k1, j, r') = let (c'
                                                  , k1) = let v8' = i
                              and v9' = 1
                              and v10' = v8' + v9'
                              and v11' = k1
                              and v12' = v10'
                              and (v13', k1) = (v12', v12')
                              in (0, k1)
                           in if c' > 0
                              then (c', k1, j, r')
                              else let (c'
                                       , j
                                       , k1
                                       , r') = let (c'
                                                   , j) = let v14' = k1
                                       and v15' = k2
                                       and v16' = v14' + v15'
                                       and v17' = j
                                       and v18' = v17' + v16'
                                       and (v19', j) = (v18', v18')
                                       in (0, j)
                                    in if c' > 0
                                       then (c', j, k1, r')
                                       else let (c', k1, r') = let (c'
                                                                   , k1) = let v20' = j
                                                and v21' = i
                                                and v22' = v20' + v21'
                                                and v23' = k1
                                                and v24' = v23' + v22'
                                                and (v25', k1) = (v24', v24')
                                                in (0, k1)
                                             in if c' > 0
                                                then (c', k1, r')
                                                else let (c'
                                                         , r') = let v26' = k1
                                                      in (3, v26')
                                                   in (c', k1, r')
                                          in (c', j, k1, r')
                                 in (c', k1, j, r')
                        in (c', k1, j, r')
                     in (c', k1, j, r')
                  in (c', j, r')
               in (c', j, r')
            in (c', i, j, r')
   in r'

cogent_ss1 : U32 -> U32
cogent_ss1 i =
   let (c', r') = let s' = i
      and s1' = s' == 0
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c', r') = let v2' = i
                  in (3, v2')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let c' = 0
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss2 : U32 -> U32
cogent_ss2 i =
   let (c', r') = let s' = i
      and s1' = True
      and (c', r') = let (c'
                         , r') = let (c'
                                     , r') = let v1' = 0
               in (3, v1')
            in (c', r')
         in if c' > 0
            then (c', r')
            else let c' = 0
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss3 : U32 -> U32
cogent_ss3 i =
   let (c', r') = let s' = i
      and (s1', s2') = (s' == 0, True)
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c', r') = let v2' = i
                  in (3, v2')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let (c', r') = let (c'
                                    , r') = let (c'
                                                , r') = let v3' = 0
                        in (3, v3')
                     in (c', r')
                  in if c' > 0
                     then (c', r')
                     else let c' = 0
                        in (c', r')
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss4 : U32 -> U32
cogent_ss4 i =
   let (c', r') = let s' = i
      and (s1', s2', s3') = (s' == 0
                            , s' == 1
                            , True)
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c', r') = let v3' = i
                  in (3, v3')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let (c', r') = let (c'
                                    , r') = if s1' || s2'
                     then let (c', r') = let v4' = i
                           and v5' = 5
                           and v6' = v4' + v5'
                           in (3, v6')
                        in (c', r')
                     else (0, r')
                  in if c' > 0
                     then (c', r')
                     else let (c', r') = let (c'
                                             , r') = let (c'
                                                         , r') = let v7' = 0
                                 in (3, v7')
                              in (c', r')
                           in if c' > 0
                              then (c', r')
                              else let c' = 0
                                 in (c', r')
                        in (c', r')
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss5 : U32 -> U32
cogent_ss5 i =
   let (c', r') = let s' = i
      and (s1', s2', s3') = (s' == 0
                            , True
                            , s' == 1)
      and (c', r') = let (c'
                         , r') = if s1'
            then let (c', r') = let v3' = i
                  in (3, v3')
               in (c', r')
            else (0, r')
         in if c' > 0
            then (c', r')
            else let (c', r') = let (c'
                                    , r') = if not s3'
                     then let (c', r') = let v4' = 0
                           in (3, v4')
                        in (c', r')
                     else (0, r')
                  in if c' > 0
                     then (c', r')
                     else let (c', r') = let (c'
                                             , r') = let (c'
                                                         , r') = let v5' = i
                                 and v6' = 5
                                 and v7' = v5' + v6'
                                 in (3, v7')
                              in (c', r')
                           in if c' > 0
                              then (c', r')
                              else let c' = 0
                                 in (c', r')
                        in (c', r')
               in (c', r')
      in (if c' == 2 then 0 else c'
         , r')
   in r'

cogent_ss6 : ( U32, U32 ) -> U32
cogent_ss6 (i, j) =
   let (c', j, i, r') = let s' = i
      and (s1'
          , s2'
          , s3'
          , s4') = (s' == 0
                   , s' == 1
                   , s' == 2
                   , True)
      and (c', j, i, r') = let (c'
                               , j) = if s1'
            then let (c', j) = let v4' = i
                  and v5' = j
                  and v6' = v4'
                  and (v7', j) = (v6', v6')
                  in (0, j)
               in (c', j)
            else (0, j)
         in if c' > 0
            then (c', j, i, r')
            else let (c'
                     , i
                     , j
                     , r') = let (c'
                                 , i) = if s1' || s2'
                     then let (c', i) = let v8' = 1
                           and v9' = i
                           and v10' = v9' + v8'
                           and (v11', i) = (i, v10')
                           in (0, i)
                        in (c', i)
                     else (0, i)
                  in if c' > 0
                     then (c', i, j, r')
                     else let (c', j, r') = let (c'
                                                , j) = if s1' || s2' || s3'
                              then let (c', j) = let v12' = 5
                                    and v13' = j
                                    and v14' = v12'
                                    and (v15', j) = (v14', v14')
                                    in (0, j)
                                 in (c', j)
                              else (0, j)
                           in if c' > 0
                              then (c', j, r')
                              else let (c', r') = let (c'
                                                      , r') = let (c'
                                                                  , r') = let v16' = j
                                          in (3, v16')
                                       in (c', r')
                                    in if c' > 0
                                       then (c', r')
                                       else let c' = 0
                                          in (c', r')
                                 in (c', j, r')
                        in (c', i, j, r')
               in (c', j, i, r')
      in (if c' == 2 then 0 else c'
         , j
         , i
         , r')
   in r'

cogent_ss7 : ( U32, U32 ) -> U32
cogent_ss7 (i, j) =
   let (c', j, i, r') = let (c'
                            , j
                            , i) = let s' = i
         and (s1', s2', s3') = (s' == 0
                               , s' == 1
                               , s' == 2)
         and (c', j, i) = let (c'
                              , j) = if s1'
               then let (c', j) = let v4' = i
                     and v5' = j
                     and v6' = v4'
                     and (v7', j) = (v6', v6')
                     in (0, j)
                  in (c', j)
               else (0, j)
            in if c' > 0
               then (c', j, i)
               else let (c', i, j) = let (c'
                                         , i) = if s1' || s2'
                        then let (c', i) = let (c'
                                               , i) = let v8' = 1
                                 and v9' = i
                                 and v10' = v9' + v8'
                                 and (v11', i) = (i, v10')
                                 in (0, i)
                              in if c' > 0
                                 then (c', i)
                                 else let c' = 2
                                    in (c', i)
                           in (c', i)
                        else (0, i)
                     in if c' > 0
                        then (c', i, j)
                        else let (c', j) = let (c'
                                               , j) = if s1' || s2' || s3'
                                 then let (c', j) = let v12' = 5
                                       and v13' = j
                                       and v14' = v12'
                                       and (v15', j) = (v14', v14')
                                       in (0, j)
                                    in (c', j)
                                 else (0, j)
                              in if c' > 0
                                 then (c', j)
                                 else let c' = 0
                                    in (c', j)
                           in (c', i, j)
                  in (c', j, i)
         in (if c' == 2 then 0 else c'
            , j
            , i)
      in if c' > 0
         then (c', j, i, r')
         else let (c', r') = let v16' = j
               in (3, v16')
            in (c', j, i, r')
   in r'

cogent_ss8 : ( U32, U32 ) -> U32
cogent_ss8 (i, j) =
   let (c', j, r') = let (c'
                         , j
                         , r') = let s' = i
         and (s1', s2', s3') = (s' == 0
                               , s' == 1
                               , s' == 2)
         and (c', j, r') = let (c'
                               , j) = if s1'
               then let (c', j) = let v4' = i
                     and v5' = j
                     and v6' = v4'
                     and (v7', j) = (v6', v6')
                     in (0, j)
                  in (c', j)
               else (0, j)
            in if c' > 0
               then (c', j, r')
               else let (c', r', j) = let (c'
                                          , r') = if s1' || s2'
                        then let (c', r') = let s' = j
                              and (s1', s2') = (s' == 0, True)
                              and (c', r') = let (c'
                                                 , r') = if s1'
                                    then let (c'
                                             , r') = let v10' = 17
                                          in (3, v10')
                                       in (c', r')
                                    else (0, r')
                                 in if c' > 0
                                    then (c', r')
                                    else let (c', r') = let (c'
                                                            , r') = let (c'
                                                                        , r') = let v11' = i
                                                in (3, v11')
                                             in (c', r')
                                          in if c' > 0
                                             then (c', r')
                                             else let c' = 0
                                                in (c', r')
                                       in (c', r')
                              in (if c' == 2 then 0 else c'
                                 , r')
                           in (c', r')
                        else (0, r')
                     in if c' > 0
                        then (c', r', j)
                        else let (c', j) = let (c'
                                               , j) = if s1' || s2' || s3'
                                 then let (c', j) = let v12' = 5
                                       and v13' = j
                                       and v14' = v12'
                                       and (v15', j) = (v14', v14')
                                       in (0, j)
                                    in (c', j)
                                 else (0, j)
                              in if c' > 0
                                 then (c', j)
                                 else let c' = 0
                                    in (c', j)
                           in (c', r', j)
                  in (c', j, r')
         in (if c' == 2 then 0 else c'
            , j
            , r')
      in if c' > 0
         then (c', j, r')
         else let (c', r') = let v16' = j
               in (3, v16')
            in (c', j, r')
   in r'

cogent_ss10 : U32 -> U32
cogent_ss10 i =
   let c' = let s' = i
      and () = ()
      and c' = gencotDummy "Unsupported switch body"
         {- return i; -}
      in if c' == 2 then 0 else c'
   in r'

cogent_ss11 : U32 -> U32
cogent_ss11 i =
   let (c'
       , r') = let c' = let s' = i
         and () = ()
         and c' = gencotDummy "Unsupported switch body"
            {- if (i == 0)
                break; -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c', r') = let v1' = i
               in (3, v1')
            in (c', r')
   in r'

cogent_ss12 : U32 -> U32
cogent_ss12 i =
   let (c'
       , r') = let c' = let s' = i
         and () = ()
         and c' = gencotDummy "Unsupported switch body"
            {- i = 5; -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c', r') = let v1' = i
               in (3, v1')
            in (c', r')
   in r'

cogent_ss13 : ( U32, U32 ) -> U32
cogent_ss13 (i, j) =
   let (c', j, r') = let s' = i
      and (s1', s2') = (s' == 0, True)
      and (c', j, r') = let (c'
                            , j) = if s1'
            then let (c', j) = let (c'
                                   , j) = let v2' = i
                     and v3' = j
                     and v4' = v2'
                     and (v5', j) = (v4', v4')
                     in (0, j)
                  in if c' > 0
                     then (c', j)
                     else let (c', j) = let v6' = i
                           and v7' = 5
                           and v8' = v6' < v7'
                           in if v8'
                              then let (c', j) = let (c'
                                                     , j) = let v9' = 1
                                       and v10' = j
                                       and v11' = v10' + v9'
                                       and (v12', j) = (j, v11')
                                       in (0, j)
                                    in if c' > 0
                                       then (c', j)
                                       else
                                          let c' = gencotDummy "Case statement only supported in direct switch body"
                                             {- 
                                             case 3:
                                             j++; -}
                                          in (c', j)
                                 in (c', j)
                              else
                                 let c' = let c' = gencotDummy "Case statement only supported in direct switch body"
                                       {- 
                                       case 1:
                                       return i + 5; -}
                                    in if c' > 0
                                       then c'
                                       else
                                          let c' = gencotDummy "Case statement only supported in direct switch body"
                                             {- 
                                             case 7:
                                             j--; -}
                                          in c'
                                 in (c', j)
                        in (c', j)
               in (c', j)
            else (0, j)
         in if c' > 0
            then (c', j, r')
            else let (c', r') = let (c'
                                    , r') = let (c'
                                                , r') = let v13' = 0
                        in (3, v13')
                     in (c', r')
                  in if c' > 0
                     then (c', r')
                     else let c' = 0
                        in (c', r')
               in (c', j, r')
      in (if c' == 2 then 0 else c'
         , j
         , r')
   in r'

cogent_ss14 : U32 -> U32
cogent_ss14 i =
   let (c'
       , r') = let c' = let s' = i
         and (s1', s2', s3') = (s' == 0
                               , s' == 1
                               , s' == 2)
         and c' = gencotDummy "Declarations not supported in switch body"
            {-
                U32 j = 0;
                
                
              case 0:
                j = i;
                
              case 1:
                i++;
                break;
                
              case 2:
                j = 5;
            -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c', r') = let v4' = i
               in (3, v4')
            in (c', r')
   in r'

cogent_ss15 : ( U32, U32 ) -> U32
cogent_ss15 (i, j) =
   let (c'
       , r') = let c' = let s' = i
         and (s1', s2', s3') = (s' == 0
                               , s' == 1
                               , s' == 2)
         and c' = gencotDummy "Declarations not supported in switch body"
            {-
                
              case 0:
                j = i;
                
                U32 k = 0;
                
                
              case 1:
                i++;
                break;
                
              case 2:
                j = k;
            -}
         in if c' == 2 then 0 else c'
      in if c' > 0
         then (c', r')
         else let (c', r') = let v4' = j
               in (3, v4')
            in (c', r')
   in r'
