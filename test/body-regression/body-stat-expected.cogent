cogent_sv1 : () -> ()
cogent_sv1 () =
   let (_
       , _
       , res') = let c' = (False
                          , False
                          , None)
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sv2 : U32 -> ()
cogent_sv2 i =
   let (_
       , _
       , res') = let c' = (False
                          , False
                          , None)
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sv3 : () -> ()
cogent_sv3 () =
   let (_
       , _
       , res') = let c' = (False
                          , False
                          , None)
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sv4 : () -> ()
cogent_sv4 () =
   let (_
       , _
       , res') = let c' = (True
                          , True
                          , Some ())
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v

cogent_se1 : U32 -> U32
cogent_se1 i =
   let (_, _, res') = let v' = i
      and c' = (True, True, Some v')
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_se2 : U32 -> U32
cogent_se2 i =
   let (_, _, res') = let v' = i
      and v1' = 5
      and v' = v' + v1'
      and v2' = i
      and v' = v' * v2'
      and c' = (True, True, Some v')
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_se3 : U32 -> U32
cogent_se3 i =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i++;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_se4 : U32 -> U32
cogent_se4 i =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i++;
             i = 5;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_se5 : U32 -> U32
cogent_se5 i =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i++;
             return ++i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v

cogent_si1 : ( U32, U32 ) -> U32
cogent_si1 (i
           , j) =
   let (_, _, res') = let v' = i
      and v1' = 0
      and v' = v' == v1'
      and c' = if v'
         then let v' = j
            and c' = (True, True, Some v')
            in c'
         else let v' = i
            and c' = (True, True, Some v')
            in c'
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si2 : ( U32, U32 ) -> U32
cogent_si2 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0)
                 return j;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si3 : ( U32, U32 ) -> U32
cogent_si3 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0)
                 i = 10;
             else
                 i += j;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si4 : ( U32, U32 ) -> U32
cogent_si4 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0)
                 i = 10;
             else if (j == 0)
                 j = i;
             else
                 i += j;
             return i + j;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si5 : ( U32, U32 ) -> U32
cogent_si5 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0) {
                 if (j == 0)
                     i = 10;
                 else
                     j += 10;
             } else if (j == 0)
                 j = i;
             else
                 i += j;
             return i + j;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si6 : ( U32, U32 ) -> U32
cogent_si6 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0) {
                 if (j == 0)
                     i = 10;
                 else
                     return j += 10;
             } else if (j == 0)
                 return j = i;
             else
                 i += j;
             return i + j;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si8 : ( U32, U32 ) -> U32
cogent_si8 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0)
                 j++;
             else
                 i++;
             j = 2 * j;
             return 2 * i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_si9 : ( U32, U32 ) -> U32
cogent_si9 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             if (i == 0)
                 j++;
             else
                 i++;
             i = 2 * j;
             return 2 * j;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v

cogent_sc1 : ( U32, U32 ) -> U32
cogent_sc1 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             j += 2 * i;
             i = i + j;
             return j * i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sc2 : ( U32, U32 ) -> U32
cogent_sc2 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             if (i > 13) {
                 j += 2 * i;
                 i += 2 * j;
             } else {
                 i++;
                 j--;
             }
             if (j == 0)
                 j = 15;
             i += j;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v

cogent_sd1 : ( U32, U32 ) -> U32
cogent_sd1 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             
             U32 k = j + 7;
             
             j += i + k;
             k += j + i;
             return k;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sd2 : ( U32, U32 ) -> U32
cogent_sd2 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             {
                 U32 i = j + 7;
                 
                 j += i + i;
                 i += j + i;
             }
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sd3 : ( U32, U32 ) -> U32
cogent_sd3 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             {
                 U32 k = j + 7;
                 
                 j += i + k;
                 k = 15;
             }
             i += j + i;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sd4 : ( U32, U32 ) -> U32
cogent_sd4 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             if (i == 0) {
                 U32 k = j + 7;
                 
                 j += i + k;
                 k = 15;
             } else
                 j = i + 3;
             i += j + i;
             return i;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sd5 : ( U32, U32 ) -> U32
cogent_sd5 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             
             U32 k1, k2;
             
             k1 = i + 1;
             k2 = j + 1;
             j += k1 + k2;
             k1 += j + i;
             return k1;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
cogent_sd6 : ( U32, U32 ) -> U32
cogent_sd6 (i
           , j) =
   let (_
       , _
       , res') = let c' = gencotDummy "Translation of nontrivial blocks not yet implemented"
         {-
             i += 2 * j;
             
             U32 k1, k2 = 42;
             
             k1 = i + 1;
             j += k1 + k2;
             k1 += j + i;
             return k1;
         -}
      in c'
   in res'
      | None -> defaultVal ()
      | Some v -> v
