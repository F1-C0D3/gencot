$ty:(MayNull (CPtr U32)) fn1($ty:(U32) i, $ty:(MayNull (CPtr U32)) p)
{
    $ty:(( U32, MayNull (CPtr U32) )) arg = {.p1 =i, .p2 =p};
    
    return cogent_fn1(arg).p1;
}
$ty:(MayNull (CPtr U32)) fn2($ty:(U32) i, $ty:(CPtr U32) p)
{
    $ty:(( U32, CPtr U32 )) arg = {.p1 =i, .p2 =p};
    
    return cogent_fn2(arg).p1;
}
